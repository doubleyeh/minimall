# dev 走 Vite proxy(见 vite.config.ts 的 server.proxy),所以 baseURL 留空 -> 同源请求
VITE_API_BASE_URL=
VITE_PROXY_TARGET=http://127.0.0.1:8080

# 仅用于"白名单外的公开接口"(如商品预览);已登录请求一律靠令牌识别租户,不依赖这个值
VITE_TENANT_CODE=platform
