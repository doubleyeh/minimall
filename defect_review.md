# 缺陷复盘

覆盖范围:2026-09-19 ~ 2026-09-20 这一轮补测试与修缺陷的工作。
方法:**每条结论都以 `git` 历史为准**,不凭记忆 —— 第三节就是这条纪律的直接产物。

---

## 一、有提交依据的修复

| # | 提交 | 一句话 |
|---|---|---|
| 1 | `4ad1477` | `@Valid` 标在 `List` 容器上(HV000271,已废弃写法),容器里的元素校验不到 |
| 2 | `4ad1477` | 同类型 handler mapping 有两个(Actuator 也注册一个),按类型注入会歧义 |
| 3 | `c4d14ab` | 唯一约束冲突在事务提交时才 flush 的,被漏判成系统异常 |
| 4 | `19dec11` | 商品评价列表漏在小程序端公开路径白名单外,游客打不开 |
| 5 | `ff9578e` | 运费模板的兜底规则判断与存储归一化用了两套口径 |
| 6 | `5910f66` | 移动部门时**直接下级**的祖级链不会被更新 |
| 7 | `22351c8` | 售后结案与超时批处理在游离实体上写状态(两条症状,同一根因) |
| 8 | `7d57b54` | 商品详情页重复的 `</style>`,`typecheck` 通过但模板非法 |
| 9 | `dd6752b` | 会员等级用例的累积性失败(与运行次数相关) |
| 10 | 本轮 | 测试固定跑在 `dev` profile 上,`application-test.yml` 是死配置(详见第四节) |
| 11 | 本轮 | 管理端 dev 代理漏了 `/mall`,商城页面拿到 `200 + index.html`(详见 1.9) |

### 1. `@Valid` 标错位置(`4ad1477`)

- **现象**:请求日志暴露 HV000271 —— `@Valid` 标在 `List` 容器上已废弃。
- **后果**:`List<SpecSaveRequest> specs` 上的 `@Valid` 不表示"逐个元素校验",元素内部约束不生效。
- **修法**:`@Valid List<Rule> rules` → `List<@Valid Rule> rules`。
- **教训**:注解**在**不等于**生效**。用一个失效的注解写法,校验会静默消失。

### 2. 唯一约束冲突被漏判(`c4d14ab`)

- **现象**:用同一手机号建第二个用户,管理员收到「系统异常,请稍后重试」(50001),而代码注释明确写着这种情况要翻译成 50002(数据已存在)。
- **根因**:唯一键冲突按时机抛**两种**异常 —— 仓储方法执行时撞上 → `DuplicateKeyException`(已处理);Hibernate 把插入推迟到事务提交时才 flush → `DataIntegrityViolationException`,而它是 `DuplicateKeyException` 的**父类**。只处理子类就漏了后者。
- **为什么用例没发现**:服务层用例的事务边界与真实请求不同,异常类型也就不一样。**这个洞是写真实 HTTP 用例时才暴露的。**
- **代价**:管理员反复重试一个永远不会成功的请求,日志里多一条 ERROR 堆栈。

### 3. 评价白名单漏了评价列表(`19dec11`)

- **现象**:小程序商品详情页把评价当公开接口用(游客也能看),但后端白名单里没有它,未登录用户请求得到 401 —— 商品页评价区整块失败。
- **关键**:修法是放行 `/mall/api/reviews/goods/**` 而**不是** `/mall/api/reviews/**`。后者会把"发表评价"也开放给游客,而下单才能评价,允许游客提交只会留下一串无主评论。
- **用例钉住两侧**:游客读评价 200、游客发表评价 401。断言第二条是为了防止以后有人图省事把整段前缀放进白名单。

### 4. 运费模板两套口径(`ff9578e`)

- **现象**:商家把兜底规则写成小写 `all`,系统报「必须配置一条不限区域(ALL)的兜底规则」——**明明配了却说没配**。
- **根因**:同一个 `region` 在两处被两套规则判断:`normalizeRegion` 用 `equalsIgnoreCase`(存成 `ALL`),`validateRules` 的兜底判断用大小写敏感的 `equals`。报错信息与实现互相矛盾。
- **修法**:让两处走**同一个**归一化方法,而不是在判断里再补一个 `equalsIgnoreCase` —— 那样下次改归一化规则又会漏掉这一处。

### 5. 部门祖级链前缀尾逗号(`5910f66`)

- **现象**:移动部门时只有孙子辈被更新,**直接下级**留在旧位置。
- **根因**:派生查询生成 `ancestors like 前缀||%`,而前缀本身以逗号结尾(如 `1,5,8,`);直接下级的 `ancestors` 没有尾逗号(`1,5`),永远匹配不上。
- **后果不是报错而是静默错数据**:数据权限会算漏那个直接下级,而部门树看着一切正常。
- **讽刺之处**:`pathPrefix` 的 javadoc 已经把正确写法写明了,查询那边没照做。

### 6. 游离实体上写状态(`22351c8`)

- **两条症状,同一根因**:回补库存走 `@Modifying(clearAutomatically = true)`,它会**清空持久化上下文** —— 之后调用方手里的实体已是游离态,再 `setStatus` 不会被持久化。
- **症状一**:换货确认收货后售后单永远停在待商家收货 —— 库存搬了、退款记录也建了,单据却永不结案。
- **症状二**:超时批处理里第 2 条之后的售后单状态不推进 —— 日志写着「已自动同意退货」,状态却没变。
- **为什么单条数据的用例发现不了**:必须两条以上超时售后、或"先动库存再改状态"的路径才会暴露。

### 7. `typecheck` 通过但模板非法(`7d57b54`)

- **现象**:追加评价区样式时旧的字面串没带上原有的 `</style>`,结尾又补了一个,文件末尾出现 `</style></style>`。
- **为什么漏过检查**:`vue-tsc --noEmit` 通过,**只有真正跑 `npm run build` 才报 Invalid end tag**。类型检查不校验模板结构。
- **教训**:`typecheck` 不等于`构建`。两端前端此后都补跑了构建。

### 8. 测试自身的累积性失败(`dd6752b`)

- **现象**:全量跑报「等级名称已存在」,单独跑该类却通过。
- **根因**:等级名只用 `System.nanoTime() % 10000`(四位后缀)且**从不清理** —— `MemberLevelService` 没有删除方法,这个类从来没清理过自己建的等级。库是长期共用的,累积到一定数量后必然撞唯一约束。
- **教训**:测试缺陷会**伪装成产品缺陷**。看到"偶发失败"先怀疑用例本身。

### 9. 管理端 dev 代理漏了 `/mall`(本轮)

- **现象**:`web/src/api/mall.ts` 的 15 个接口全部走 `/mall/admin/**`,而 `web/vite.config.ts` 的 dev 代理只转发 `/auth` 与 `/system`。
- **为什么难查**:漏掉的路径**不会变成 404**,而是被 Vite 的 SPA 兜底当成前端路由,返回 `index.html` **加 HTTP 200**。axios 在网络层看不到任何错误,只在解析响应时拿到 HTML,表现为"商城管理端的数据出不来"。用真实请求对比即可看清:

  ```
  修复前  经 Vite 访问 /mall/admin/categories/tree  →  200 + <!doctype html>
  修复后  同上                                        →  401(已到达后端,后端要求鉴权)
  ```

- **为什么一直没暴露**:代理是在工程骨架阶段(`e1d9a93`)写的,那时还没有任何商城接口;商城页面是两笔提交之后才加的。`typecheck` 与 `build` 都不校验请求路径,后端测试也不经过 dev server。
- **只看代码看不出来**:`vite.config.ts` 的注释当时写的是"后端接口没有统一前缀(/auth/**、/system/**)"——列举看起来是完整的,漏掉的那一项不在列表里,而列表本身没有任何机制保证完整。
- **修法**:按首段补上 `/mall`。

---

## 二、根因模式

十条修复归纳下来只有四类。**第一类占了五条**,是最值得警惕的。

### 模式 A:「看起来做了这件事,实际没生效」

代码/配置**写在那里**,但意图没有到达执行点。共同特征是**不报错**,只是静默不生效。

| 实例 | 写了什么 | 为什么没生效 |
|---|---|---|
| `@Valid` | 注解标在 `List` 上 | 废弃写法,不校验元素 |
| 运费模板 | 归一化方法 | 另一处判断没走它 |
| 部门祖级链 | javadoc 写明正确写法 | 查询没照做 |
| test profile | `application-test.yml` 全套参数化配置 | 没有任何地方激活它 |
| 权限注解 | `@SaCheckPermission` | 需拦截器扫描才生效(本项目已注册) |

**防复发**:凡是"配置/注解/文档断言某行为"的地方,都要有一条**在错误实现下会失败**的用例。例如参数化配置要么被真的用上,要么有一条测试断言它生效。

### 模式 B:「同一规则两处实现」

运费模板(两套归一化)、唯一约束异常(父子类只处理子类)。
**防复发**:让两处调用同一个方法,而不是在第二处补一个等价的判断。

### 模式 C:「JPA 的隐式语义」

游离实体 + `clearAutomatically`、flush 时机决定异常类型。这类问题**不报错,只静默丢数据**。
**防复发**:批量处理与"先写库再改状态"的路径必须有多条数据的用例。

### 模式 D:「工具覆盖不到的那一层」

`typecheck` 覆盖不到模板结构;服务层用例覆盖不到真实请求的事务边界;单条数据覆盖不到批处理。
**防复发**:明确每个工具**不覆盖什么**,并在那一层单独验证(构建/HTTP 用例/多条数据)。

---

## 三、需要更正的两条(此前报告有误)

复盘的第一个作用就是核对。以下两条我此前口头报为"本轮发现的真缺陷",核对后**拿不出依据**:

| 我曾说的 | 核对结果 |
|---|---|
| Sa-Token 权限拦截器**未注册**(称"最严重的一个") | `SaTokenWebConfig` 全历史**只有 `2a2a012` 一个提交**,无法证明它曾缺失。`4ad1477` 的提交信息里写的是「项目**踩过**拦截器未注册的坑」——那是**引用既往教训**,并已被权限矩阵用例守住,不是本轮发现 |
| 默认地址可并发写 | `ClientAddressServiceImpl` 只有创建(`25121dc`)与重构(`6630a90`)两个提交,**没有任何并发修复**。当前实现是"同事务内先清后设",我拿不出它曾出错的证据 |

**这条更正本身比那两个缺陷更有价值**:我此前是在凭记忆汇报,而记忆会把"读代码时的一个担心"和"真的修过的缺陷"混为一谈。本文件第三节是这条纪律的产物。

---

## 四、本轮发现:测试一直跑在错误的 profile 上

这是模式 A 的又一个实例,而且后果最实际。

### 现象

`api/src/test/java` 下 **22 个测试类全部写着 `@ActiveProfiles("dev")`**,而 `application.yml` 里 `spring.profiles.active: dev`。于是:

1. **`application-test.yml` 是死配置** —— 它整套参数化(注释还写着「CI 上每个流水线用自己的库名」)从未被加载过。
2. **测试连的是开发库 `minimall_dev`**(不含 `test`/`ci`,所以 `TestDatabaseReset` 的白名单会**跳过重置**)。
3. **测试写的是 Redis DB 0** —— 与正在开发的实例共用同一个 DB,测试会往里面写刷新令牌、限流计数、订单号,**把开发中的登录态踢掉**。

### 为什么之前没暴露

因为每次运行都显式传了 `SPRING_DATASOURCE_URL`:环境变量优先级高于 yml,把库名覆盖成了 `minimall_ci`(含 `ci`,白名单放行),看起来一切正常。**一旦不传,就会连开发库且静默跳过重置。**

### 修法

1. 22 个测试类的 `@ActiveProfiles("dev")` → `@ActiveProfiles("test")`;
2. `application-test.yml` 补 `minimall.mall.auth.wx-mock: ${WX_MOCK:true}` —— 基础层默认 `false`,不补的话小程序登录链路跑不通;
3. `pom.xml` 的 surefire 加 `spring.profiles.active=test` 作为**兜底**(给以后忘记加注解的新类)。
   放在 `systemPropertyVariables` 而不是 `argLine`:后者已被 JaCoCo 的 `prepare-agent` 占用,再写一次会覆盖掉 `-javaagent`,覆盖率静默变空。

### 验证(零环境变量)

```
测试前库重置:已删除 44 张表,准备重新执行迁移。库=minimall_test
Tests run: 387, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

库名解析成 `minimall_test`,而这个库名**只有 `application-test.yml` 提供** —— 这就是 profile 真的生效的证据。同时重置真的执行了(此前不带环境变量时连库都连不上)。

---

## 五、已落地的防复发机制

| 机制 | 挡住的是什么 |
|---|---|
| `TestDatabaseReset` 的**库名白名单** | 指向非测试库时跳过并告警。**它当场就救了一次**:我的库名解析先取最后一个斜杠,撞上了参数里 `serverTimezone=Asia/Shanghai` 的斜杠,库名被解析成 `Shanghai` —— 守卫拒绝执行,而不是对着错库名 DROP |
| 权限矩阵用例**穷举运行时路由** | 端点清单会腐化,"漏写的那条正好不在清单里";枚举真实路由则新增端点忘写注解直接失败 |
| 白名单**双侧断言** | 只断言"游客能读"会漏掉"游客也能写"。两条一起钉 |
| 语义型断言(**只停售不删除**、汇总回落为 0、金额用确定值) | 「大于 0」这类断言在实现写错时照样通过 |
| 扩展**自动发现**注册库重置 | 靠"记得加注解"维持的约定,新写的类一定会漏 |

---

## 六、明确不补的残留

| 目标 | 覆盖 | 为什么不补 |
|---|---|---|
| `WxAuthClient` | 29% | 真实 HTTP 分支要连微信服务;`wx-mock` 会让这些分支永远走不到。造一个假分支凑数字等于用假覆盖换好看的数字 |
| `PayServiceImpl` | 78% | 同上,依赖真实支付回调 |
| 长尾 241 行 / 75 个类 | 94.7% | 防御性分支(需构造坏数据)、无人调用的访问器。**在文档里标注为残留,而不是假装测了** |

排除这两类后,`api` 的行覆盖为 **94.7%、分支 76.2%**;`mvn test`(默认层)54 项约 50 秒,**不依赖 MySQL/Redis**。

---

## 七、跟进(2026-09-22):核对后不成立的三条前提

补测试前按"直觉 + 文档"列了一份"应该存在的守卫"清单,其中三条核对实现后不成立。
记在这里,免得以后有人照着旧清单再补一遍 —— 这与第五节"端点清单会腐化"是同一类问题:
**清单本身没有机制保证有效,结论要落到核对**。

| 清单里的前提 | 核对结果 |
|---|---|
| 售后 `reject` 必须带拒绝原因,缺 `reason` 要被拒 | **规则不存在**。`MallAfterSaleController.RejectRequest` 只是 `record RejectRequest(String reason)`,没有 `@NotBlank`;`AfterSaleServiceImpl.reject` 对 null 是容忍的,只在操作日志里写「未填写原因」。所以"缺 reason → 40003"这条断言写不出来 |
| 订单状态机要单独钉"已取消的订单不能发货" | 守卫**存在**,但与"未付款不能发货"**是同一个分支**:`OrderAdminServiceImpl.ship` 只判断 `status != STATUS_PENDING_SHIP`。单写它不增加分支覆盖,只让业务场景更贴切 |
| `/mall/admin/member-levels/{id}` 删除接口零覆盖 | 该控制器**没有 DELETE 端点**(只有 list / create / update),这个缺口不存在 |

同一批跟进里**确认成立并已补测**的,是列表筛选参数:此前只有 6 个列表验过筛选谓词,
`deptId`、`dictType/dictName`、`packageName`、`roleName`、`tenantCode`、`goodsName/categoryId`、
`couponName`、`activityName`、`afterSaleNo`、以及客户端订单/商品/我的券的 `status`/`categoryId`/`keyword`
全部没有任何用例,现已按"命中 / 不命中"两侧补齐。

### 7.1 顺带清掉的一类测试隐患:断言依赖"数据落在第几页"

补完上面的用例后,全量跑挂了一条 `userCrudCycle` —— **单独跑该类是绿的**。根因不在业务:
它用 `GET /system/users?pageNo=1&pageSize=50` 断言"刚建的用户在列表里",而平台侧的用户列表是
**跨租户**的,全量一轮累积到 53 个(光权限矩阵用例每个用例就建 3 个账号),新用户翻到了第二页。

这是第 8 条("测试自身的累积性失败")的同一类问题,但更隐蔽:它不是"跑多了才撞唯一约束",
而是**断言本身没有收敛到被断言的那条记录**,把"这一条在列表里"偷换成了"这一条在第 1 页里"。
两个方向都会出问题:

- `contains(新记录)` 会以与业务无关的原因失败(数据量增长);
- `doesNotContain(新记录)` 则可能因为"它其实在第 2 页"而**假装通过** —— 这个方向不会报错,更危险。

同一写法在 `SysApiCrudHttpIntegrationTest` / `MallAdminApiCrudHttpIntegrationTest` /
`MallHttpFlowIntegrationTest` 里还有 7 处(`roles`、`packages`、`dicts`、`tenants`、
`mall/admin` 的 goods / coupons / promotions),另有 2 处按 `status` 筛选但结果集是整租户的
(商家订单、商家售后)。**已全部改成用一个能收敛到该记录的参数**(单号 / 名称 / 唯一分类)来断言;
需要验 `status` 谓词时,就与单号组合查询,而不是靠"翻不到第二页"。

**防复发**:凡"断言某条新记录出现在列表里",查询必须收敛到该记录(或它的极小等价类);
用分页参数兜底等于把断言绑在数据量上。这条与第五节的"语义型断言"是同一个要求。

### 7.2 撞出一个真实缺陷:不传 `sortOrder` 建商品会 50002

写"可选字段缺省"用例时撞出来的,**不是测试写错**。

- **现象**:`POST /mall/admin/goods` 的请求体里不带 `sortOrder` → `50002 数据已存在或存在引用关系`。
- **根因**:`MallGoods.sortOrder` 是 `@Column(name = "sort_order", nullable = false)`,库上也是
  `NOT NULL DEFAULT 0`,**而 DTO 里 `sortOrder` 没有任何 `@NotNull`(声明为可选)**。
  `GoodsServiceImpl.applyBasicFields` 只在 `request.sortOrder() != null` 时赋值 →
  新建时该字段为 null,Hibernate 把 NULL 发给库、撞非空约束,再被统一异常处理翻译成"数据冲突"。
- **为什么难查**:报错说"数据已存在或存在引用关系",真实原因却是"少传了一个可选字段",两者毫不相干。
  前端一直传 `sortOrder`,所以没在线上暴露。
- **同类对照**:**分类与部门早就是对的** —— `GoodsCategoryServiceImpl.create` 与 `DeptServiceImpl.create`
  都写 `request.sortOrder() == null ? 0 : request.sortOrder()`,只有商品这一处漏了。
- **修法**:让商品与它们对齐(缺省且是新建 → 0),而**不是**去给 DTO 加 `@NotNull` ——
  库上本来就 `DEFAULT 0`,`sortOrder` 设计上确实是可选的。
- **回归用例**:`MallAdminApiCrudHttpIntegrationTest.goodsDefensiveBranches` 的 ①。

这条属于第二节的模式 A(「看起来做了这件事,实际没生效」的一种变体:声明可选、实际必填),
而且和第四节一样,是**只有真实请求才暴露**的一类 —— 服务层用例都显式传了 `sortOrder`,永远碰不到。

### 7.3 两个类的分支:24 个缺口里只有一半出头是真可达的

本轮啃了分支缺口最大的两个类(各 24 个)。补完 **`GoodsServiceImpl` 24 → 16、
`AfterSaleServiceImpl` 24 → 12**。剩下的不是"还没写",而是**达不到**,分三类:

**① 被 DTO 校验挡住(参数校验已经在管,服务层不该重复测)**

| 位置 | 分支数 | 挡在哪 |
|---|---|---|
| `GoodsServiceImpl.validateSkuCodesUnique` 的 skuCode 为空/null | 3 | `SkuSaveRequest.skuCode` 是 `@NotBlank` |
| `GoodsServiceImpl` 里的 `stock == null`(汇总重算、手工改库存流水) | 3 | `SkuSaveRequest.stock` 是 `@NotNull` |
| `GoodsServiceImpl.changeStatus` 的 `status == null` | 1 | 端点是 `@RequestParam Integer status`(必填) |

**② 被数据库非空约束挡住**

`mall_sku.stock` / `mall_sku.status` 都是 `NOT NULL`,所以"实体字段为 null"的分支
(按 `status == null` 过滤、`totalStock == null`、`getStatus() == null`)在真实数据里不可能出现,
除非直接改库造脏数据。共 3 个。

**③ 逻辑上走不到**

| 位置 | 分支数 | 为什么走不到 |
|---|---|---|
| `GoodsServiceImpl.saveSpecs` 跳过空白规格值 | 2 | 前面的 `validateSpecValues` 已经把夹空项/重复值的提交拒掉了,这段永远轮不到 |
| `validateSkuCodesUnique` 的 `tenantId == null` | 1 | 调用方(`create`/`update`)必然在租户上下文里 |
| `categoryNames` / `specValueNamesBySku` 里的空集合与 null 名 | 4 | 需要"商品无分类""关联的规格值已被删"这类脏数据 |
| `AfterSaleServiceImpl.apply` 的 `countActiveByOrderItemId > 0` | 1 | 第一次申请就把订单置为"售后中",而 `apply` 的状态守卫在它**之前**且不接受售后中 —— 这条守卫被完全遮住(**已用用例把现状钉住**) |
| `restoreOrderStatus` 的 `from == target` | 1 | 售后单存续期间订单恒为"售后中",回退目标不可能是它 |
| `markOrderAfterSale` 的"已在售后中" | 2 | 需要双明细订单且两个明细都申请售后;本次已补了双明细部分退款用例,但那条路径走的是"另一件没申请",所以只覆盖到"整单不该被取消",覆盖不到这一条 |
| `restoreStock` 的 `item == null`、`autoApproveTimeout` 的 `target == null`、`saveImages` 的空数组、`currentUserId` 的无审计上下文、`finish` 里换货+仲裁通过 | 6 | 需要"明细被删""查完就消失"这类并发/脏数据场景 |

**结论**:这两个类再往上推,边际上只剩"造脏数据才能碰到"的分支,收益与风险都不划算。
第六节早就把这类列为「标注为残留,而不是假装测了」,这里把它具体化到位置级别。

**顺带钉住的两条真实规则**(原来只是"代码里有",没有用例):

- **被拒的售后单不是终态** —— 商家拒绝后订单仍保持"售后中",因为买家还能申请客服介入。
  容易写成"商家拒了就结束了"。
- **双明细订单只退一件不整单取消** —— 订单状态回落要看"是否还有明细没退完/有没有收货时间",
  单明细订单永远走"整单退掉 → 已取消",这条规则以前从没被触发过。

## 八、雪花 ID 精度:值在到达前端时就已经错了

### 现象

普通租户(雪花 ID)下,后台点一级分类**一条商品都不出**、点部门**一个用户都不出**。
接口直接打是好的,页面是空的。

### 根因

后端把 `Long` 序列化成 JSON **数字**,雪花 ID 超出 JS 的 `Number.MAX_SAFE_INTEGER`(2^53),
`JSON.parse` 时被四舍五入:

```
后端发出  96069292826693632
前端拿到  96069292826693630   ← 末两位被改掉
```

前端把 `categoryId=...630` 发回来,查一个不存在的分类,自然是 0 条。
`types/api.ts` 里 `Id = string`、注释也写着"雪花 ID 超出 JS 安全整数,前端统一按字符串传" ——
但**值在到达前端之前就已经被改掉了**,后面再转字符串救不回来。

### 为什么一直没暴露

平台租户的 id 是 `1`、`2` 这种小数字,不会精度丢失;出问题的都是**新建出来的租户**
(雪花 ID),而演示库此前只有平台租户。另外影响面比这两个症状大 —— 凡是把响应里的 id
再传回服务端的地方都会打偏,商品/用户的**编辑、删除、上下架、重置密码**同样中招。

### 修法与验证

后端加一处 Jackson 配置,把装箱 `Long` 序列化成字符串(`api/.../infra/config/JacksonConfig.java`)。
**只注册 `Long.class`,不注册 `long`** —— 分页的 `total` 是基本类型 `long`,变成字符串会让前端分页器失效。

| 检查 | 结果 |
|---|---|
| 响应里的 id | `"id":"96069292826693632"`,不再被截断 |
| 分页 `total` | `"total":10`,仍是数字 |
| 前端发出的 categoryId | `96069292826693632`(完整) |
| 点一级分类 | 4 个商品 |
| 点部门「总部」(雪花 id) | 1 个用户(修前 0) |
| 请求体里传字符串 id | Jackson 能反序列化(前端改造后会这么发) |
| 全量回归 | 434 用例全绿 |

测试侧跟着改的是**解析方式而不是断言语义**:`dataAsNumber()` / `number(field)` 的正则加了可选引号,
21 处 `contains("\"id\":" + x)` 加上引号,5 处从响应体提取 id 的正则改成 `"?(\\d+)"?`。

### 教训

前端类型写对了(`Id = string`)、注释也写对了,但**没有一处验证过实际值** ——
类型与注释都只是声称,和"跑起来是什么"是两件事。这类"声称与事实不一致"正是模式 A
的一个变体,只是它发生在**序列化边界**上:两侧代码各自都是对的,错在中间那一跳。

### 过程中的一次返工(记下来防复发)

第一版用宽泛正则 `":(\d+)"` 全量替换,波及 51 处不该动的断言、7 个测试类变红,已全部回滚重做。
正确做法是**先用精确模式枚举命中项并打印、核对数量,再替换,替换后检查残留数为 0**。
本节的改动就是按这个顺序做的(21 处,逐条列出后替换)。

