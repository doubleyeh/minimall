# 单体应用脚手架完整方案:RBAC + 多租户

本文档是唯一交付物,配合 `rbac_tenant_schema.sql`(建表脚本),足够任何模型/开发者据此独立完成开发和测试,不依赖其他上下文。全文只描述职责、契约、数据流、取舍理由,不含可直接编译的实现代码——具体代码由实现方自行编写。

---

## 0. 范围与非目标

**范围**:身份认证对接、多租户隔离、RBAC(菜单/按钮权限 + 数据权限)、审计、ID生成、异步场景的租户上下文传播。

**明确不做**(现阶段决策,不是遗漏):
- **不做业务模块**:支付、商品、订单等一律不在本版范围内,本版只交付基础设施(认证/隔离/权限/审计/异步上下文)。
  因此 6.1 支付回调**只保留设计不实现**(它的价值在于说明"没有登录态时租户从哪来"的判定顺序,后续做业务时照此落地即可)
- 不做软删除,物理删除
- 配置(数据库密码等)明文写入 `application.yml`,不接配置中心/密钥管理
- 不引入 MQ,不做跨进程消息队列的租户透传
- 不用 `ScopedValue` 替代 ThreadLocal(JDK25 才转正,本项目 JDK21)
- 不做 Schema 级/独立数据库级租户隔离(见第2节)

---

## 1. 技术栈

| 层 | 选型 | 版本/说明 |
|---|---|---|
| 语言/运行时 | JDK 21 | 虚拟线程默认开启 |
| 框架 | Spring Boot | 4.1.x(当前最新次要版本) |
| ORM | Spring Data JPA + QueryDSL | 不用 MyBatis-Plus,不用 Spring Data `Specification`(理由见1.1) |
| 数据库 | MySQL 8 | |
| 鉴权 | Sa-Token | 登录态、踢人下线 |
| 缓存 | Redis | 会话存储、权限/字典缓存,key 统一加租户前缀 |
| ID生成 | 雪花算法 | 应用层生成,不用数据库自增(理由见4.3) |

### 1.1 ORM 选型理由
- QueryDSL 生成编译期类型安全的 Q 类,动态条件用 `BooleanBuilder` 拼接,可读性优于 `Specification` 的 lambda 嵌套
- QueryDSL-JPA 生成的是 JPQL,和 Hibernate `@Filter` 机制天然兼容,过滤器对其自动生效
- 落地边界:简单 CRUD 用 Spring Data JPA 派生方法;涉及租户+数据权限+业务条件叠加的动态查询用 `JPAQueryFactory` + QueryDSL

### 1.2 虚拟线程注意事项
- 禁用 `synchronized`,一律用 `ReentrantLock`,否则虚拟线程被钉在载体线程上失去意义
- 数据库连接池大小按数据库承载能力配置,不按虚拟线程数量放大(JDBC 驱动本身阻塞)
- MDC(traceId)基于 ThreadLocal,虚拟线程下工作正常,**但不能跨线程传播**——`@Async`/线程池提交的任务里 MDC 是空的,需要自定义 `TaskDecorator` 显式拷贝(见 7.2 的 traceId、6.3 与 4.12 的审计上下文)

---

## 2. 租户隔离模型

| 方案 | 隔离强度 | 成本 | 适用场景 |
|---|---|---|---|
| 共享库 + tenant_id 字段隔离(**采用**) | 弱,依赖代码不出错 | 低 | 租户数量多,无强合规要求 |
| 共享库 + 独立 Schema | 中 | 中,需多schema路由 | 租户数量中等 |
| 独立数据库 | 强 | 高 | 金融/医疗等强合规场景 |

**风险兜底**(字段隔离没有数据库层保护,靠以下措施控制风险):
1. 隔离性回归测试作为 CI 必跑项(见第8节),不是可选项
2. 数据库连接账号最小权限,禁止应用层执行未审查的 DDL/批量操作
3. Native SQL 和 JPQL 的 bulk update/delete **不受**自动过滤保护,团队规范强制要求手写租户条件,或禁止对租户表使用这两种方式

---

## 3. 工程结构 ✅

**仓库布局**:`mini_mall` 是**项目根**,只放设计产物(`architecture.md`、`rbac_tenant_schema.sql`)与各服务目录;`mini_mall/api` 是 **java_api 服务的服务根**——它有自己的 `pom.xml` 与 `src/main/...`,构建、启动、CI 都以它为工作目录(`cd mini_mall/api && mvn ...`)。以后再有 `job`/`web` 之类的新服务,在项目根下平级新增目录,互不干扰;项目根**不放聚合 pom**,避免"根 pom 决定所有服务构建方式"这种隐式耦合。

**决策:单 Maven 工程(单 jar),不拆多模块**。理由:本方案是单体应用,现阶段 `api/service/domain/infra/common` 之间没有"独立部署/独立复用"的需求,拆多模块只增加构建复杂度(pom 互相依赖、IDE 索引、发布链路),收益为零。依赖方向用一条 ArchUnit 测试固化(见 9.1),效果等价而成本更低。**将来某个域确实需要独立发布时再拆,且按域拆而不是按层拆**。

包结构(命名空间前缀统一为 `com.{company}.mall`):

```
api       接口层,只放 Controller + DTO,不写业务逻辑,禁止直接注入 Repository
service   业务逻辑,面向接口编程(接口与实现分包)
domain    实体 + 仓储接口(Repository 接口声明在这里,实现由 Spring Data 生成)
infra     租户过滤、权限校验、审计、ID生成、缓存、外部对接等技术实现
common    统一响应、全局异常、工具类、常量
```

- 业务域作为**第二级子包**纵向切:`...api.sys`、`...service.sys`、`...domain.sys`,以后的 `mall` 域同理。这样"按领域纵向切"和"按技术层横切"两件事不冲突:层是主干,域是分支。
- **依赖方向单向**:`api → service → domain`;`infra` 被 `api`/`service` 依赖,不反向依赖它们(避免循环);`common` 可以被任何人依赖,但它自己不依赖任何人。违反即构建失败,不允许注释掉这条测试。

---

## 4. 多租户隔离机制

### 4.1 上下文载体:TenantContext ✅

职责:持有当前线程的 `tenantId`(允许为空)和 `isSuperUser` 标志,基于 ThreadLocal。

**三种状态必须区分清楚**,4.2 的过滤启用规则直接依赖它:

| 状态 | tenantId | isSuperUser | 典型场景 |
|---|---|---|---|
| 未定租户 | null | false | `/auth/login`、`/pay/callback/**`、健康检查——不涉及租户表数据 |
| 已定租户、未登录 | 非空 | false | 带 `X-Tenant-Code` 的公开接口(见 4.9) |
| 已登录(含超管) | 非空 | 见右 | 普通请求;超管用户 `isSuperUser = true`(见 4.10) |

**`isSuperUser` 是用户级,不是租户级(相对前一版的重要修正)**:该标志**取自 `sys_user.is_super`,不再取自 `tenant.is_super`**。原设计把"跳过租户过滤"挂在租户上,等于该租户下**每一个**用户(包括普通员工)的请求都不加 `tenant_id` 条件,安全性完全落在 `data_scope` 上——而默认管理员角色的 `data_scope = 5`,即超管租户的一个普通员工就能看全平台数据。改成用户级后,"谁能跨租户"精确到账号。建表脚本已同步调整(`tenant.is_super` 删除,`sys_user.is_super` 新增)。

接口契约:
- `setTenantId(Long)` / `getTenantId(): Long`(允许 null)
- `setSuperUser(boolean)` / `isSuperUser(): boolean`
- `clear()`:请求/任务结束时必须调用,避免线程池复用导致串租户
- `runAsTenant(Long tenantId, boolean superUser, Runnable action)`:在指定租户上下文中执行一段逻辑,执行完自动恢复外层上下文(支持嵌套调用),用于没有 HTTP 请求周期的场景(见第6节)

设计要点:测试时直接调用 set 方法注入即可验证依赖它的逻辑,不需要起真实 HTTP 请求,这是全套隔离机制可测试性的基础。**另外**,审计所需的操作人信息由独立的 `AuditContext` 承载(见 4.12),不要塞进 `TenantContext`:两者生命周期与传播范围不同(异步场景下前者需要显式传参,后者靠事务边界重新应用)。

### 4.2 过滤生效机制:Hibernate Filter ✅

**为什么选它,不选 SQL 拦截层方案(如 MyBatis-Plus 的 TenantLineInnerInterceptor)**:Hibernate Filter 是 ORM 原生机制,声明式加在实体基类上,对 JPQL / Criteria / QueryDSL-JPA 全部生效,包括懒加载关联对象;SQL 拦截层本质是解析 SQL 文本重写,关联查询、复杂 join 有额外边界情况需要维护。

机制描述:
1. 在实体基类上声明过滤器定义(`@FilterDef`)和过滤条件(`@Filter`,条件为 `tenant_id = :tenantId`)。声明在 `@MappedSuperclass` 基类上后由子类继承(Hibernate 6/7 的行为);**不走基类的实体就没有过滤**,这种实体必须单独声明,或者(更推荐)改成走基类,清单见 4.6.1
2. **默认拒绝,只有超管放开(相对前一版的重要修正)**。原设计是"超管或未登录公开接口都不启用过滤器",漏掉了"忘记设置上下文"这个最常见的事故:一旦 `TenantContext` 为空,过滤器不启用,查询直接变成跨租户全表扫描。新规则只有两条:
   - `isSuperUser = true`:不启用(**这是唯一的豁免**,语义是"确实要跨租户看数据")
   - 其他所有情况:一律启用。`tenantId` 非空时绑定该值;**`tenantId` 为空时绑定一个恒假条件(`tenant_id = -1`)**,让租户表查询返回空集而不是全集。这样漏设上下文的表现是"查不到数据"(启动/联调阶段立刻暴露的 bug),而不是"看到所有人的数据"(数据泄露)
3. **enable 的时机是关键**:必须发生在每次 Session 创建/事务开启之后。**实现时的结论是:HTTP 路径也走事务切面,不在 servlet filter 里 enable** —— servlet filter 阶段 `OpenEntityManagerInViewInterceptor` 还没执行,请求上根本没有绑定 Session,在那里 enable 的过滤器会被随后创建的 Session 覆盖掉。所以"**每个事务边界重新 enable**"是唯一正确的落点,同步与异步一视同仁。**这是本方案里最容易被实现者忽略、但必须测试覆盖的一点**(见 8.1 用例 6)。
4. **过滤器只作用于查询,不作用于主键直查(实测踩坑,安全级约束)**:`EntityManager.find()`、也就是 Spring Data 默认的 `findById`,生成的是 `select ... where id = ?`,**不带任何租户条件与数据权限条件**。只靠它做"按 ID 加载",7.3 的越权防护等于没落地:任何登录用户拿到一个 ID 就能读到/改到别的租户、别的部门的行。落地方式:`SysUserRepository`/`SysRoleRepository`/`SysDeptRepository` 都用 `default` 方法覆盖 `findById`,内部改走 QueryDSL 查询(`findOne(Q.id.eq(id))`)让过滤器生效。**新增带过滤器的实体仓储时必须照做**(见 4.6.1 的清单);确需跨过滤读取的场景要显式表达(超管上下文或专用方法),不能靠主键直查绕过去。代价是每次多一条 SQL、不再吃持久化上下文的一级缓存,这个取舍是刻意的。

组件划分:
- `TenantFilterService`:独立的过滤开关逻辑,不依赖 HTTP,提供"根据当前 TenantContext 状态,在传入的 EntityManager 对应 Session 上 enable/disable 过滤器"的方法。单独抽出来的好处是测试可以直接调用(8.1 用例 6 就是这么验证的),不用起真实请求。
- `TenantWebFilter`(Servlet Filter,**只做识别与校验,不做过滤器启用**):HTTP 入口,从 Sa-Token 会话取 tenantId/isSuperUser 写入 TenantContext、校验租户状态(4.11)、按 4.9 的规则处理未登录公开接口、构造 AuditContext(4.12),请求结束的 finally 里清理两个上下文。它**不调用** TenantFilterService,原因见上面第 3 点。
- 租户过滤切面(AOP):在**每个事务边界**调用 TenantFilterService 应用当前上下文,同时覆盖 HTTP 与异步路径,是过滤器生效的**唯一入口**。由此得到一条硬约束(见下面的切点说明):service 实现类必须统一标注 `@Transactional`,否则该入口的查询会在"过滤器未启用"的状态下执行——那不是报错,是跨租户读到数据。
- **数据权限参数的计算顺序也是一处坑**:`TenantFilterService` 必须**先 `disableFilter(dataScopeFilter)` 再去算参数**。因为算参数要用 `sys_user` 读当前用户的部门与角色,而 `sys_user` 恰好是数据权限过滤的目标表;HTTP 路径下 open-in-view 让整个请求共用一个 Session,上一轮事务 enable 过的过滤器会**带着旧参数**继续生效,一旦旧参数是"拒绝",这次就查不到用户 → 参数又退化成"拒绝" → 后续所有查询都返回空集。现象是"页面突然什么数据都没有",根因却在两次事务之间的状态残留。

**切面顺序必须给具体值,不能靠默认值(前一版只写了"要注意",而实现时就是在这里翻车)**。Spring 的 `@Transactional` advisor 默认 order 是 `Ordered.LOWEST_PRECEDENCE`(最低优先级 = 最内层);如果租户切面也用默认值,两者的相对顺序**未定义**,租户切面可能跑在事务之外,此时 `EntityManager.unwrap(Session.class)` 拿到的不是绑定到事务的那个 Session,enable 的结果会被随后真正的 Session 覆盖掉,隔离静默失效。落地方式:

- 显式指定事务管理器的 order:`@EnableTransactionManagement(order = 0)`
- 租户切面:`@Order(10)`,order 数值**大于**事务 advisor,才能保证它在事务边界**内侧**执行,拿到的是已绑定到事务的 Session
- 切点同时匹配**方法级与类级**:`@annotation(Transactional) || @within(Transactional)`,不用包名通配。只写方法级会漏掉"只在类上标注"的入口,而漏掉的后果不是报错,是过滤器没启用、查询直接跨租户。本方案要求 **service 实现类统一在类上标注 `@Transactional`**,并用 ArchUnit 测试守门(见 9.1 的 archunit 依赖)
- **硬约束:任何需要访问租户表的入口(HTTP 的 service 方法、支付回调、定时任务的处理单元)都必须落在带 `@Transactional` 的类或方法里**。没有事务就没有 Session 边界,过滤器无处可挂——这是租户隔离能生效的前提,不是可选项
- `spring.jpa.open-in-view` **显式配置为 `true`,三环境一致**(见 9.3),不依赖框架默认值:它的默认值在启动日志里是带 WARN 的(属于被诟病的反模式),将来若被改为 `false`,HTTP 路径会变成"每次 Repository 调用一个新 Session",入口 enable 一次就不再够用,而故障表现是"偶发看到其他租户的数据",极难排查

### 4.3 主键策略:雪花算法 ✅

不用数据库自增,理由:多租户场景下自增 ID 容易被猜测出别的租户数据量/增长趋势;为将来可能的分库分表留出空间。

生成器设计:
- workerId 来源:优先读部署时指定的环境变量(`SNOWFLAKE_WORKER_ID` / `SNOWFLAKE_DATACENTER_ID`,多实例部署必须显式配置,避免撞车);未配置时默认 1,仅适用于单机开发。**不按网卡地址取模**——MAC 变化会让同一实例换号,而它并不能真正防住多实例撞车,不如直接要求显式配置,并在启动时校验范围(两者都是 5 bit,取值 0-31,越界启动即失败)
- 标准雪花结构:41 bit 时间戳 + 5 bit datacenterId + 5 bit workerId + 12 bit 序列号,同一毫秒内序列号用完则自旋等下一毫秒;检测到时钟回拨直接抛异常拒绝生成(宁可报错也不生成可能重复的 ID)。取锁用 `ReentrantLock`,不用 `synchronized`(理由见 1.2 的虚拟线程 pinning)
- **和 JPA 集成(实现时相比上一版有调整)**:不再通过 Hibernate 的 `IdentifierGenerator`/`@IdGeneratorType` SPI 接入,改为实体基类的 `@PrePersist` 回调里"id 为空则生成"。理由:`org.hibernate.generator.*` 这套 SPI 在 Hibernate 6→7 之间改动频繁(接口方法签名、`@IdGeneratorType` 注解方式都变过),把 ID 生成这种纯业务能力绑在 ORM 内部接口上,升级 Hibernate 就要跟着改;`@PrePersist` 是标准 JPA,换 ORM 也不受影响,效果一致(持久化前生成、不依赖数据库自增)。代价是**不能**用 `em.persist` 之外的 bulk insert 绕过回调——本方案本来就不用 bulk insert 写业务数据(见 2 节的风险兜底)

### 4.4 审计字段自动填充 ✅

`create_time`/`update_time`/`create_by`/`update_by` 通过 Spring Data JPA Auditing 机制自动填充,不需要业务代码手动赋值:
- 时间字段:标准 JPA Auditing 注解标记,框架自动填
- 操作人字段:**取值来源是 `AuditContext`(见 4.12),不是直接读 Sa-Token 会话**。前一版写的是"若 Sa-Token 会话已登录则取当前用户ID",这在异步落库(7.2 的操作日志)和异步任务里必然失效——那些线程里既没有 Sa-Token 会话也没有 `TenantContext`。改为读 `AuditContext` 后,同步请求由 `TenantWebFilter` 在入口填充、异步场景由调用方显式传入快照(见 4.12),同一套取值逻辑覆盖两种路径
- 取不到时仍然写 null(表定义允许),但**有数据权限需求的业务表例外**:这类表的归属人不允许为 null,见 5.3 的归属人约束

### 4.5 tenant_id 自动回填 ✅

新增实体时,若业务代码没有显式设置 `tenant_id`,由一个实体监听器在持久化前自动从 `TenantContext` 取值回填;若 `TenantContext` 里也没有(理论上不应该出现,除非监听器本身被跳过),直接抛异常阻断,不允许 tenant_id 为空的脏数据写入。这个兜底是为了不依赖每个业务代码都记得手动传 tenant_id。

**唯一的例外,必须显式声明**:`sys_oper_log` 在"租户未知"时允许写入 —— 典型是登录失败日志,此时还没识别出租户,但这条日志恰恰是排查撞库/锁定问题时最有价值的。实现方式不是"碰巧没触发校验",而是 `BaseTenantEntity` 上的一个显式钩子(`tenantRequired()` 返回 false),子类覆盖它时同时要确认建表脚本里该表的 `tenant_id` 确实是可空的(否则会在数据库层再失败一次)。除了这一类实体,任何 `tenant_id` 为空的新增数据都应该继续抛异常阻断。

### 4.6 实体基类:两个,不能混用 ✅

| 基类 | 适用对象 | 特征 |
|---|---|---|
| `BaseTenantEntity` | `sys_user`、`sys_dept`、`sys_role`、`sys_oper_log`,以及以后的所有业务表 | 带租户过滤 + 审计字段,tenant_id 非空,自动回填 |
| `BaseAuditEntity` | `tenant`、`sys_menu`、`sys_package`、`sys_package_menu`、`sys_tenant_package_change`、`sys_dict_type`、`sys_dict_data`、`sys_pay_account` | 只有审计字段,不带租户过滤,平台级数据 |

`sys_role.tenant_id` 现在**始终非空**——不再有"可空表示平台模板"的例外(见 4.7 套餐机制,新租户的初始角色由套餐生成,不再依赖可复用的平台角色模板)。`sys_role` 因此可以直接用标准的 `BaseTenantEntity`,不需要特殊查询逻辑。

`sys_oper_log` 归入 `BaseTenantEntity`(它有 `tenant_id`,按租户查日志是合理需求),但**写入路径不能依赖 4.5 的自动回填**(异步线程里上下文是空的),必须显式赋值,见 4.12。

#### 4.6.1 每张表的隔离方式(实现前先对这张表)

| 表 | 基类 | 租户过滤 | 数据权限 | 说明 |
|---|---|---|---|---|
| `tenant` | `BaseAuditEntity` | 否 | 否 | 租户清单本身是平台级数据;禁用/过期校验见 4.11 |
| `sys_user` | `BaseTenantEntity` | 是 | **是**(按 `dept_id`) | 用户列表要按部门范围看,见 5.3 |
| `sys_dept` | `BaseTenantEntity` | 是 | 否 | 部门树是组织架构而非业务数据,租户内全员可见;能不能改由菜单权限控制 |
| `sys_role` | `BaseTenantEntity` | 是 | 否 | 角色列表若要按部门分权,由业务侧显式叠加,不进默认过滤 |
| `sys_menu` | `BaseAuditEntity` | 否 | 否 | 全平台统一;谁能看到哪些菜单由 `sys_role_menu` 决定;`is_platform = 1` 表示平台专用菜单(见 4.10) |
| `sys_user_role` / `sys_role_menu` / `sys_role_dept` | 无(纯关联表) | 否 | 否 | **没有 `tenant_id` 列,不受任何自动过滤**。隔离依赖两条:①只允许用"当前租户内的 `user_id`/`role_id`"构造查询,这些 ID 来自已被过滤的查询结果;②任何直接按 ID 操作的接口都要先做一次带过滤的存在性校验(见 7.3) |
| `sys_oper_log` | `BaseTenantEntity` | 是 | 否 | 超管查全平台日志时靠 `isSuperUser` 豁免 |
| `sys_package` / `sys_package_menu` / `sys_tenant_package_change` / `sys_dict_type` / `sys_dict_data` / `sys_pay_account` | `BaseAuditEntity` | 否 | 否 | 平台级配置/字典/审计,只有超管可管理;访问控制靠"这些接口的菜单权限只授给平台租户的角色"(见 4.10) |

这张表的作用是免去逐处推理:**新增表时必须先在这里加一行,再写实体**。凡是"这张表要不要加过滤、为什么"的问题,答案都在上面。

---

### 4.7 套餐机制:租户的初始权限从哪来 ✅

**职责边界**:套餐(`sys_package`)定义的是"一个租户能用哪些菜单/功能",是**租户级的功能开通范围**(entitlement),和 RBAC 的角色权限是两层不同的概念——套餐决定天花板,角色决定天花板以内怎么分配给具体用户。不要把两者混在一起设计。

**数据结构**:
- `sys_package`:套餐本身,平台级数据(超管维护),不分租户,用 `BaseAuditEntity`
- `sys_package_menu`:套餐-菜单关联表,定义一个套餐包含哪些菜单/功能点
- `tenant.package_id`:租户表已有此字段(建表脚本里从一开始就预留了),创建租户时绑定套餐

**租户创建流程**(需要在一个事务里完成,任何一步失败都要整体回滚):
1. 创建 `tenant` 记录,绑定 `package_id`。**套餐必填**(不接受为 NULL;"不限"只是兼容历史数据的路径,见 4.8 步骤 1)
2. 为该租户创建默认管理员角色(`sys_role`,`tenant_id` = 新租户ID,`data_scope` = 5-全部,`is_default` = 1。租户内的管理员默认能看该租户全部数据是合理的)
3. 查 `sys_package_menu` 拿到该套餐包含的全部菜单ID
4. 把这些菜单ID全部写入 `sys_role_menu`,绑定到第2步创建的管理员角色
5. 创建该租户的**根部门**(`sys_dept`,`parent_id = 0`、`ancestors = ''`、名称取"租户名 + 总部"之类)。**这一步不能省**:`data_scope = 2/3/4` 全都依赖部门层级,没有根部门的话,该租户新建的用户要么没部门可挂,要么一挂上就被数据权限挡成"看不到任何数据"(见 5.3)
6. 创建默认管理员用户(`sys_user`),绑定到第2步的角色、绑定到第5步的根部门。**`is_super` 必须显式写 0**(租户管理员不是平台超管,见 4.10);初始密码按 7.1.2 处理——系统随机生成时置 `must_change_password = 1`,明文只在创建响应里返回一次

这样"新租户能看到哪些菜单"完全由套餐决定,不再需要平台角色模板这种可复用但语义复杂的机制。

**套餐与已建租户的关系**(前一版这里写的是"套餐只在创建时生效一次,后续变化需另走流程",现在明确为):**套餐的任何变化都会立即同步到已绑定租户**——租户换套餐走 4.8,平台直接改套餐内容走 4.8.1,两者共用同一套差异计算规则(新增只给默认管理员角色,收回对全部角色立即生效)。不存在"改了不生效"的中间状态。

---

### 4.8 套餐变更流程 ✅

**触发时机**:超管在管理端修改 `tenant.package_id`(升级或降级)。

**新增授权 vs 收回授权,处理方式不对称,这是有意为之**

- **新增(升级,套餐里多了菜单)**:只自动同步到该租户 `is_default = 1` 的默认管理员角色(建表脚本 `sys_role` 新增该字段,作为不受业务改名影响的机器标识)。自定义角色不动,需要租户管理员自己手动分配——理由:新增权限是"锦上添花",不主动帮自定义角色加,顶多是少了点便利,不会有安全风险
- **收回(降级,套餐里少了菜单)**:**直接、立即、对该租户下的全部角色生效**——不区分默认角色还是自定义角色。理由:套餐是租户级的功能边界(entitlement),降级意味着这个租户从平台层面就不该再摸到这些功能了,如果只收默认角色、放过自定义角色,等于降级没有真正生效,任何一个绕过默认角色的自定义角色都还能访问,这是安全/合规意义上不能接受的口子。上一版设计里"宽限模式 + 运行时二次拦截"的复杂方案就是为了兜这个口子,现在直接在变更时刻把权限收干净,不产生"套餐外但角色仍关联"的中间状态,也就不需要再叠加运行时二次校验这层复杂度了

**处理流程**(一个事务内完成,任何一步失败整体回滚,和 4.7 建租户流程同一种事务边界要求):

1. 查询新旧套餐各自的菜单集合:`M_old`(变更前 `package_id` 对应的 `sys_package_menu`)、`M_new`(目标 `package_id` 对应的)。**`package_id` 为空表示"不限",按"全部 `is_platform = 0` 的菜单"处理**:NULL→某套餐 等于对全量做一次收回、某套餐→NULL 等于对全量做一次新增、NULL→NULL 直接跳过不产生变更记录。`sys_tenant_package_change.old_package_id` 为 NULL 即表示"变更前不限"。**建租户接口要求套餐必填**(不接受创建 NULL),NULL 只是兼容"不限"状态的路径,否则后面每次变更都要处理一次全量差异
2. 计算差异:`added = M_new - M_old`(新增授权)、`revoked = M_old - M_new`(套餐收回的菜单)
3. 找到该租户 `is_default = 1` 的角色,把 `added` 里的菜单ID写入 `sys_role_menu`(已存在的跳过,幂等)
4. 收回:`DELETE FROM sys_role_menu WHERE menu_id IN (revoked) AND role_id IN (SELECT id FROM sys_role WHERE tenant_id = 当前租户)`——这一条 SQL 覆盖该租户下所有角色,不管是默认角色还是自定义角色,不做例外
5. 写入 `sys_tenant_package_change` 审计记录:`old_package_id`、`new_package_id`、`added_menu_ids`、`revoked_menu_ids`——纯粹是审计留痕,不再需要"模式"字段,因为收回动作本身就是唯一确定的行为,没有分支
6. 更新 `tenant.package_id`

**需要覆盖的测试用例**(补充到 8.1):
- 用套餐 P1(菜单集合 M1)创建租户后,变更为套餐 P2(菜单集合 M2 ⊂ M1),断言:默认管理员角色和该租户下所有自定义角色的 `sys_role_menu` 里,都不再包含 `M1 - M2` 这部分菜单
- 升级场景(M2 ⊃ M1):断言新增菜单只出现在默认管理员角色,自定义角色的菜单集合变更前后一致(不受影响)
- 变更记录表 `sys_tenant_package_change` 的 `added_menu_ids`/`revoked_menu_ids` 计算结果和实际生效的差异一致

#### 4.8.1 平台修改套餐内容(触发源二):改了马上生效 ✅

**决策**:超管直接修改 `sys_package_menu`(给套餐增减菜单)时,**立即对当前绑定该套餐的所有租户生效**——不做定时同步、不做版本快照、不引入"待生效"状态。理由:套餐是 entitlement 天花板,改了不生效等于系统里长期存在一个"配置与实际行为不一致"的窗口,而窗口本身没有任何业务价值;实现代价也可以很低——复用 4.8 已有的差异计算与写入逻辑,只把触发源从"改 `tenant.package_id`"换成"改 `sys_package_menu`",把作用对象从一个租户换成"所有绑定该套餐的租户"。

**处理流程**:

1. 修改 `sys_package_menu` 前,先把该套餐**旧的菜单 ID 列表读出来作为 `M_old`**(`tenant.package_id` 这次没变,`M_old` 只能靠这个快照);修改后的列表即 `M_new`
2. 查 `SELECT id FROM tenant WHERE package_id = :该套餐`,得到受影响的租户列表
3. 对列表里**每个租户各起一个事务**(不是一个覆盖全部租户的大事务),执行 4.8 的差异流程:新增菜单只写默认管理员角色(`is_default = 1`),收回菜单对该租户全部角色执行 delete,规则与 4.8 完全一致,不重新定义
4. 每个租户写一条 `sys_tenant_package_change`(`trigger_type = 2`,`old_package_id = new_package_id = 该套餐`)
5. **单个租户失败不影响其他租户**:失败只记日志 + 告警(该租户停留在未同步状态),不中断循环、不回滚已成功的租户。这与 6.2 定时任务的处理哲学一致——多租户批量操作必须按租户切事务
6. 全部处理完(含失败项)统一失效相关租户的权限缓存,见 5.5

**没有采用"运行时按套餐实时计算菜单"这种看起来更省事的做法**,理由:角色授权是独立的维度(租户要在套餐范围内自己分配),如果每次权限校验都直接回落到套餐,自定义分配就失去意义了;而且 4.8 的"降级时对全部角色收回"也没有了落点。`sys_role_menu` 是权限的事实来源,套餐只是它的合法边界,这条边界靠 5.2.1 的授权校验 + 本节/4.8 的同步共同保证。

**为什么"立即生效"是安全可行的**:收回是幂等 delete,新增是幂等 insert-if-absent(4.8 步骤 3 已要求),两者都可重复执行,所以中途失败后重跑同一个套餐的同步一定能收敛到正确状态,**不需要补偿机制**;但需要"重跑入口"——提供一个超管可触发的"套餐权限重新同步"操作(对某套餐/某租户重跑差异计算),这是低成本且必要的运维出口,列入 8.4 的测试范围。

### 4.9 公开接口与"未定租户"的请求 ✅

前一版只说了"公开接口不启用过滤",没有回答"这些接口怎么知道自己在服务哪个租户"。规则如下,**核心是禁止出现"匿名且未定租户"却能读租户表的接口**:

- **白名单**:只有以下路径允许在没有 `tenantId` 的情况下执行,且各自有明确的租户识别方式——`/auth/login`(请求体带 `tenantCode`,见 7.1.1)、`/pay/callback/**`(从商户订单号解析,见 6.1)、健康检查/actuator。白名单以显式配置维护,不用"路径里含 auth 就放行"这类模糊匹配
- **其他需要公开访问的业务接口**(商品预览、活动页等):必须由前端显式带租户标识——请求头 `X-Tenant-Code: acme`,由 `TenantWebFilter` 解析成 `tenantId` 写入上下文。这类请求是 4.1 表里的第二态(已定租户、未登录),**过滤器照常启用**,只是不做登录态校验
- **兜底**:既不在白名单、又没有租户标识、又未登录的请求,在进入业务逻辑前直接拒绝(403)。配合 4.2 的"未定租户用恒假条件",即使有遗漏的入口,最坏结果也只是"查不到数据",不会出现跨租户读取
- `X-Tenant-Code` 只用于**定位租户**,不代表任何身份,不能因此跳过菜单/数据权限校验;需要数据的接口该登录还是要登录
- **过滤器顺序是硬约束(实测踩坑后补)**:`TenantWebFilter` 要在 filter 阶段读 Sa-Token 会话,而 Sa-Token 的请求上下文由它自己的过滤器建立(`SaTokenContextFilterForJakartaServlet`,order = **-104**)。因此**本过滤器的 order 必须晚于 -104**,否则 `StpUtil` 会抛 `SaTokenContextException: 上下文尚未初始化`——现象是**每个请求都 500**(不是某个接口 401/403),极具迷惑性。所以不要写 `HIGHEST_PRECEDENCE + n` 这种"越早越好"的值,实现里固定为 -103 并在代码里写明了这个数字的来历

### 4.10 平台超管账号:独立于租户管理员 ✅

**为什么单列一节**:超管(能跨租户看数据、能管套餐和租户)和"某个租户的管理员"是两种角色。前一版把"跳过租户过滤"做成租户级标志,等于"超管"这个概念没有精确的账号载体。这一版明确:

- **超管 = `sys_user.is_super = 1` 的账号**,属于平台租户(种子数据里 `tenant_code = 'platform'` 的那条,见 9.5),不属于任何业务租户
- 超管的登录方式与普通用户完全一样(同一套 `/auth/login`、同样校验租户状态、同样有 `lock_time` 逻辑),区别只在登录成功时往会话里写 `isSuperUser = true`
- **超管的菜单/按钮权限**:`StpInterface` 实现中对 `is_super = 1` 的账号短路返回全部 `sys_menu` 的 `perm_code`(`status = 1`),**不走角色计算**。理由:超管的权限集合就是"全部",走角色只会引入"平台租户的角色被误改导致超管进不去后台"这类运维事故,没有收益
- **超管的数据权限**:`isSuperUser = true` 时租户过滤不启用(见 4.2),`data_scope` 对超管不生效(它的语义是"租户内的可见范围",超管的语义是"跨租户可见")
- **超管不能做的事**(写出来,避免实现者自由发挥):不能通过接口创建 `is_super = 1` 的账号(该标志只能由种子数据或运维脚本设置,接口不接受这个字段);不能禁用/删除平台租户;不能跳过 4.11 的租户状态校验
- **平台级接口的防线是两层**:①平台专用菜单在 `sys_menu` 上标 `is_platform = 1`,而 5.2.1 已规定这类菜单不允许进任何套餐,所以普通租户在数据库层面就取不到这些权限;②`is_super` 身份校验。第一层是主要防线,第二层是兜底

### 4.11 租户状态变更与在线会话 ✅

**问题**:`tenant.status = 0`(禁用)与 `expire_time` 过期,前一版只在登录时校验(7.1.1 第 1 步)。已经在线的会话不受任何影响——一个 7200 秒超时的会话可以继续正常访问,最长 2 小时才自然消失。停用租户必须立刻生效,否则这个功能等于没有。`sys_user.status = 0`(禁用用户)是同一类问题,处理方式一致。

**设计**:

- `TenantWebFilter` 在每个请求里除从会话取 `tenantId`,还要校验租户状态:**先查 Redis 缓存 `tenant:status:{tenantId}`(值为 `status|expireTime`,TTL 60 秒),miss 再查库回填**。不每次查库是为了避免把 `tenant` 表变成每个请求的热点
- 校验不通过(禁用/已过期/记录不存在)→ 与 7.1.1 第 1 步同一种处理:`StpUtil.logout()` 清理会话,返回 **401 + 统一文案**("租户状态异常"),不进入业务逻辑
- **立即生效路径**:超管禁用租户或修改有效期时,**先删 Redis 的 `tenant:status:{tenantId}`,再落库**。删缓存后该租户所有用户的下一个请求就会 miss 并查库,拿到新状态后判定失败并登出——**惰性踢下线,不需要维护"该租户在线用户列表"这类索引**,代价是"立即生效"的粒度是一个请求
- 60 秒 TTL 的取舍:即使删缓存这一步被漏掉(例如有人直接改库),最长 60 秒后也会收敛。这是有意留的兜底,不是精确性要求
- **超管自己不受这条规则限制**(平台租户永不过期/不会被禁用),否则平台租户被误禁用会导致谁都进不了后台
- 租户**不做物理删除**(见 5.6),因此不存在"记录被删掉"这条分支

### 4.12 审计上下文快照:AuditContext ✅

**要解决的问题**:操作日志要异步落库(7.2),但异步线程里既没有 `TenantContext` 也没有 Sa-Token 会话;而 `sys_oper_log` 的 `tenant_id`/`user_id` 是必填。前一版把"当前操作人"的来源写成"读 Sa-Token 会话",和异步落库这个要求直接冲突——同步请求里取得到,异步线程里取到的是空,结果要么写不进去(非空约束),要么写出一批不知道是谁干的日志。

**设计**:引入一个纯数据的不可变对象 `AuditContext`(含 `tenantId`、`userId`、`ip`、`traceId`),两个用途都不依赖"当前线程恰好有会话":

- **HTTP 请求**:`TenantWebFilter` 在入口统一构造一次,放进请求作用域的 ThreadLocal(与 `TenantContext` 同生命周期,filter 的 finally 里一起清理)。4.4 的 JPA Auditing 从它取值
- **异步/无请求场景**:**把 `AuditContext` 作为参数显式传给异步方法**(或由提交任务时捕获进 lambda),异步线程里用它显式赋值 `tenant_id`/`user_id`/`create_by`/`update_by` 后再 `save`。5.3 的归属人字段同样从它取值

**关键约束(必须测试覆盖,见 8.1)**:

- 异步写 `sys_oper_log`/业务表时,**不要靠 `runAsTenant` 去"修好"上下文再写**:`runAsTenant` 只解决 Hibernate 过滤与 4.5 的 `tenant_id` 自动回填,`create_by` 这类字段仍然取不到(异步线程没有 Sa-Token 会话)。显式赋值是唯一可靠方式
- 显式赋值时**必须同时给 `tenant_id` 赋值**,否则 4.5 的监听器会抛异常阻断——这是设计意图而非缺陷:让"忘了传审计快照"变成启动/联调即可暴露的错误,而不是写出 `tenant_id` 不明的脏数据
- 快照只在"请求入口"或"异步任务提交点"捕获一次,不在异步线程内部重新推断身份(推断不出来,也没有必要)

---

## 5. RBAC 与权限模型

> 说明:前一版的章节编号有缺陷——`5.1` 之前没有一级标题,`5.1`~`5.4` 实际挂在 4.8 下面。这里补上一级标题,编号不变,不引用位移。

### 5.1 数据表 ✅

见 `rbac_tenant_schema.sql`:`tenant`、`sys_dept`、`sys_user`、`sys_role`、`sys_menu`、`sys_role_menu`、`sys_role_dept`、`sys_user_role`、`sys_package`、`sys_package_menu`、`sys_tenant_package_change`、`sys_oper_log`、`sys_dict_type`、`sys_dict_data`、`sys_pay_account`。

要点:
- `sys_menu` 全平台统一,不分租户,租户角色只是勾选授权,不能自建菜单;`is_platform = 1` 的菜单是平台专用,只给平台租户,不允许进入任何套餐(见 4.10、5.2.1)
- `sys_dept.ancestors` 冗余祖级链(逗号分隔),支持"本部门及以下"用一条 `LIKE` 查询,不用递归
- **唯一约束集中声明在脚本里**(`sys_role(tenant_id, role_key)`、`sys_menu.perm_code`、`sys_dept(tenant_id, dept_name)`、`sys_user(tenant_id, phone)` 等,清单见 9.7),应用层只做"友好提示"不做唯一性保证——"先查后插"有并发窗口,唯一约束是最后一道且可靠
- **不使用外键约束**,引用完整性由 service 层承担,见 5.6 与 9.7

### 5.2 两个正交的权限维度 ✅

1. **菜单/按钮权限**:能不能进页面、能不能点按钮。`sys_menu.perm_code` 标识,格式 `模块:资源:操作`(如 `order:delete`)。
2. **数据权限**:同一操作,返回数据的范围。`sys_role.data_scope` 枚举:1-仅本人 / 2-本部门 / 3-本部门及以下 / 4-自定义部门 / 5-全部。落地机制:复用 4.2 的 Hibernate Filter 方式,单独声明一个 `dataScopeFilter`,在 `TenantFilterAspect` 同一个切面里跟租户过滤一起 enable,不再引入 AOP 拼 SQL 这第二套机制。菜单/按钮权限则相反,直接用 Sa-Token 自带的 `@SaCheckPermission` 注解(本质是 AOP),不用重新实现。

两者独立校验,不要耦合进同一段逻辑——菜单权限决定"能不能调这个接口",数据权限决定"这个接口返回什么范围的数据",职责不同。

**关于 `@SaCheckPermission` 的两个硬约束(实测踩坑后补的)**:

1. **注解必须配合拦截器才生效**。`@SaCheckPermission` 不是 Spring 自带的机制,它由 Sa-Token 的 `SaInterceptor` 在进入 Controller 前扫描 HandlerMethod 上的注解来完成(见 `SaTokenWebConfig`)。只写注解不注册拦截器,注解就只是一行注释:**接口照旧返回 200,任何登录用户都能调用** —— 权限矩阵用例里"没有角色列表权限"的用户确实拿到了 200,补上拦截器后同一请求返回 403。这类漏洞不会报错、不会告警,只能靠"角色 × 接口"的用例矩阵发现(8.2)
2. **鉴权拒绝必须同时设置 HTTP 状态码**。只把响应体里的 `code` 改成失败是不够的:前端与网关最先看的是状态码,监控上"拒绝"与"成功"也会混在一起。约定与过滤器里的拒绝保持一致:**401 = 未登录、403 = 已登录但无权限**,由 `GlobalExceptionHandler` 落地。业务异常同样遵守这条:`BusinessException` 带着 `UNAUTHORIZED`/`FORBIDDEN` 时返回 401/403(例如"刷新令牌不可用"),其余业务错误仍是 200 + 业务码(7.3)——否则刷新失败这类响应在网关上与成功请求无法区分

#### 5.2.1 角色授权的边界:可分配菜单 ⊆ 套餐菜单 ✅

**这是前一版最实质的漏洞,单独列出来。** 4.8 的套餐收回机制(降级时对全租户角色 delete)成立的前提是"角色里不可能存在套餐外的菜单";但套餐只是"租户能用哪些菜单"的声明,**如果授权接口允许租户管理员把任意 `sys_menu` 勾给自定义角色,这个前提就不成立**:降级时 `revoked` 只清"本次收回的那些",管理员之前手动加的菜单不在 `revoked` 里,会被留下来继续生效——收回白做。而且这不是攻击行为,是管理员日常操作的必然结果。

**四条规则,都必须落在接口上**:

1. **候选集**:角色授权接口返回的菜单树,必须按该租户 `tenant.package_id` 查 `sys_package_menu` 过滤后再返回。`package_id` 为空(不限)时返回全部 `is_platform = 0` 的菜单。平台专用菜单(`is_platform = 1`)永远不出现在候选集里
2. **保存校验**:写 `sys_role_menu` 时服务端**再次校验**提交的每个 `menu_id` 都在候选集内,越界直接返回业务异常——**不做静默过滤**。静默过滤会让"前端显示的授权"与"实际生效的授权"不一致,排查成本远高于直接报错
3. **默认管理员角色的菜单由套餐同步维护,不接受人工增删**(`is_default = 1`)。它是套餐的投影,人工改过之后又会被套餐同步"修正",停在半对半错的状态;租户管理员要定制权限请建自定义角色(建表脚本里 `is_default` 注释已说明该字段是"不受业务改名影响的机器标识")
4. 同理,`sys_package_menu` 保存时校验:不含 `is_platform = 1` 的菜单;且**菜单树的父链必须完整**(勾了子菜单就要包含其全部祖先,否则前端渲染出的菜单树会断,见 5.1 的 `sys_menu` 结构)

**测试**:8.1 第 13 条专门覆盖"越界授权必须被拒绝";8.2 的权限矩阵要包含"租户管理员试图给角色加套餐外菜单"。

### 5.3 数据权限条件映射表 ✅

**多角色合并规则:取最大值(即最宽的那一档 `data_scope`)。** 用户通过 `sys_user_role` 可以持有多个角色,每个角色各有一个 `data_scope`;计算时收集该用户全部 `status = 1` 的角色,取 `data_scope` 的**最大值**(5 最大 = 最宽),然后**只按最大值那一档生成条件**。例:同时持有"仅本人"和"全部"两个角色 → 按"全部"处理,不追加任何条件。

为什么取最大而不是最严:角色是授权来源,给一个用户挂上"全部"角色本身就是一次明确的授权决定,系统不该在运行时悄悄打折;这也与菜单权限的并集语义一致(多个角色的 `perm_code` 取并集)。**注意这个规则的副作用**:`data_scope` 的宽松方向是"看得更多",所以分配角色时的默认值要谨慎——4.7 里给建租户的默认管理员角色配 `data_scope = 5` 是刻意的,其他新建角色的默认值建议用更窄的档位。

**两个边界(实测踩坑后补的规则)**:

1. **身份前置查询不适用数据权限**。登录、刷新令牌换票发生在"身份确认之前",此刻 `AuditContext` 里没有 userId,"算不出来就拒绝"的兜底会给出 `denyAll`,把按 `tenant_id + username`(或按 ID)读 `sys_user` 的语句拦成空集——现象是**账号密码都对却提示用户名或密码错误**,而服务层单测因为自己先绑了审计上下文,完全看不出来(只有真 HTTP 链路能暴露)。落地方式:这两个入口用 `DataScopeBypass` 显式声明"本轮跳过数据权限过滤"。**豁免的只是数据权限,租户过滤照常生效**(4.1 的租户隔离任何情况下都不豁免),且该开关只允许用在这两处
2. **自己的账号永远可读**。`sys_user` 用的条件比通用条件多一条 `or id = 当前用户ID`。原因:"仅本人"档按 `create_by = 当前用户` 判断,而**用户不是自己创建的**,于是该档用户连自己那一行都读不到——改密、读自己的权限快照会全部失败,报错却像"用户不存在"。这不与本节冲突:数据权限约束的是"能看别人哪些数据",不是"能不能看自己"。注意这一支要求 `currentUserId` 有值:完全没有身份时(上一条)仍是真空集;而"用户存在但没有任何有效角色"这种情形返回 `denyAllButSelf` —— 带上用户 ID,保证他至少能读到自己(否则连初始密码都改不了,会卡死在"能登录、什么都做不了")
3. **`ancestors` 的 LIKE 前缀两端都要逗号**。第 3 档的前缀形如 `1,5,8,`(祖级链 + 本部门 ID + 逗号),对应的条件必须写成 `concat(子部门.ancestors, ',') like 前缀 || '%'`:直接子部门的 `ancestors` 是 `1,5`(没有尾逗号),不补逗号就**永远匹配不到直接子部门**,只查到孙部门及更深;补上尾逗号还顺带挡住"部门 12 与 123 前缀相同"这类误匹配。前缀拼接统一走 `SysDept.pathPrefix(...)`,不要在数据权限与部门移动两处各写一份(实测踩过:两份里错了一份)

| data_scope | 追加的查询条件 |
|---|---|
| 1-仅本人 | `create_by = 当前用户ID` |
| 2-本部门 | `dept_id = 当前用户所在部门ID` |
| 3-本部门及以下 | `dept_id IN (SELECT id FROM sys_dept WHERE ancestors LIKE '当前部门ancestors%')` |
| 4-自定义部门 | `dept_id IN (SELECT dept_id FROM sys_role_dept WHERE role_id IN (当前用户所有 data_scope = 4 的角色ID))` |
| 5-全部 | 不追加条件 |

补充约定:

- **适用范围见 4.6.1**:只有标了"数据权限 = 是"的表参与,当前就是 `sys_user` 和以后的业务表。给系统表(角色/部门/菜单/字典)套数据权限会得到没有业务意义的结果(例如"只看自己创建的角色"),不做
- **归属人字段的语义与硬约束**:`data_scope = 1` 依赖 `create_by`,而它在业务表上的含义是"数据归属人"而非"谁插入的"。因此对参与数据权限的业务表加一条硬约束:**`create_by` 写入时必须非空**,由 4.5 的回填逻辑校验(取不到审计上下文就抛异常,见 4.12)。必然结果是——定时任务/回调创建的业务数据必须显式带上归属人,否则插入失败。这是有意的:一条没有归属人的数据在当前模型下对所有人都不可见,与其静默写入不如直接拒绝
- **`dept_id` 为空的用户**:按 2/3/4 档生成的条件命中空集(`NULL = x`、`NULL IN (...)` 都不成立),表现为"看不到任何数据"。**不要为了"友好"把空部门特判成"看全部"**,那是把配置疏漏放大成越权
- **4 档的部门集合取并集**:该用户所有 `data_scope = 4` 的角色在 `sys_role_dept` 里的部门合并,与其他角色配置叠加符合"取最大"的直觉

### 5.4 请求处理链路(同步 HTTP 请求) ✅

```
TenantWebFilter:租户识别(会话 / X-Tenant-Code)→ 校验租户状态(4.11,带缓存)
              → 写 TenantContext + AuditContext
              → 应用租户过滤(4.2,未定租户用恒假条件)
  → Sa-Token 鉴权(登录态,白名单路径跳过)
  → 菜单/按钮权限校验:比对 perm_code(来源见 5.5),不通过返回 403
  → 强制改密拦截:must_change_password = 1 时,除改密/登出/刷新权限外一律 403(见 7.1.2)
  → 数据权限拦截:按 5.3 的映射表生成条件,与租户过滤同一批 enable
  → Service / Repository 执行
```

**顺序要求**:租户过滤必须先圈定该租户可见的数据全集,数据权限在这个全集基础上进一步缩小范围。顺序反了,"超管 + 仅本人"这类组合会拿不到正确的部门归属信息(超管不启用租户过滤,但数据权限仍按 5.3 生效)。

**已确定**:菜单/按钮权限走 Sa-Token 自带 `@SaCheckPermission`(AOP),权限来源与缓存失效见 5.5;数据权限复用 4.2 的 Hibernate Filter 机制(`dataScopeFilter`,和租户过滤同一个切面里 enable,切面顺序见 4.2),不再是开放项(见 5.2)。

### 5.5 权限的存储、缓存与失效 ✅

**问题**:`@SaCheckPermission` 只是校验入口,权限数据从哪来、改了权限什么时候生效,前一版完全没写。这块不定义清楚,权限变更的生效时间就是不可预测的。

**存储与读取**:

- **登录态**(`tenantId`、`isSuperUser`、`userId`)放在 Sa-Token 会话扩展数据里,由 4.2 的 `TenantWebFilter` 每请求读一次。数据极小,不做额外缓存
- **权限集合**(`menus` + `permCodes`)不放进 Sa-Token 会话,单独放 Redis:`perm:v{version}:{tenantId}:{userId}`。`StpInterface.getPermissionList()`/`getRoleList()` 从这里读,miss 时查库回填并设 TTL
- **超管短路**:`is_super = 1` 直接返回全量 `perm_code`,不查缓存也不查角色(见 4.10)

**失效方式:租户级版本号,不做按 key 批量删除。**

- Redis 里维护 `perm:version:{tenantId}`(一个整数,无 TTL),所有权限缓存 key 都带这个版本号
- **任何一次权限相关写入,只做一件事:`INCR perm:version:{tenantId}`**。旧版本号的 key 再也没人访问,靠 TTL 自然过期,不需要扫描、不需要删除
- 这样做的理由:①`SCAN` + `DEL` 在 key 多时开销大且不原子,删不干净就是"权限已收回但用户还能用",属于安全问题;②版本号是 O(1) 且天然正确,不受"到底该删哪些用户"的影响;③实现只有一个 `INCR`,不容易写错

**触发失效的操作(全部收敛到同一个入口,不要散落在各 service 里)**:

| 操作 | 需要 INCR 的租户 |
|---|---|
| 角色-菜单授权变更(`sys_role_menu`) | 该角色所属租户 |
| 用户-角色变更(`sys_user_role`) | 该用户所属租户 |
| 套餐变更 / 平台改套餐内容(4.8、4.8.1) | 受影响的每个租户各一次 |
| 菜单被禁用/删除、`sys_package_menu` 变更 | 全部租户(菜单是平台级数据) |
| 角色被禁用/删除 | 该角色所属租户 |
| 用户被禁用 | 该用户所属租户 |

**生效时机**:版本号一变,该租户**下一个请求**就是新权限,**不需要踢人下线**。这是"缓存 key 带版本号"而不是"权限放进会话"的主要收益:权限收紧立即生效,而会话本身(登录态)不动,用户体验不受影响。

**前端快照**:登录响应里的 `menus`/`permCodes` 是那一刻的快照(7.1.1)。权限变更后前端要拿到新菜单,靠两种方式:重新登录,或调用 `GET /auth/permissions` 拉一次最新权限(服务端同一套逻辑;前端在收到 403 时调用一次即可)。**不做主动推送**,现阶段没必要。

**TTL 取值**:权限缓存 TTL 与 Sa-Token 会话超时对齐(见 9.3),避免出现"会话还在但权限缓存已过期"的抖动;版本号 key 不设 TTL。

### 5.6 删除与引用完整性 ✅

**前提**:本方案不做逻辑删除,全部物理删除,**且不使用数据库外键**(见 9.7)。因此级联清理与引用保护全部由 service 层承担,必须逐条明确,不能靠"实现者应该会想到"。

**通用规则**:

- 所有删除/解绑在一个事务内完成,先清理关联表再删主记录
- 删除前先做引用检查,存在引用时**返回业务异常并说明被什么引用**,不做静默级联删除(下表明确列出的关联表除外)
- 没有外键兜底,任何"删了主表但漏删关联表"都会留下孤儿记录,所以关联表清理必须写在删除主记录的同一个方法里,**不允许在 Controller 层拼装**

| 删除对象 | 处理方式 |
|---|---|
| 用户 | 校验:不能删自己;不能删该租户最后一个默认管理员角色的持有者(否则该租户失去管理入口)。级联删 `sys_user_role`。历史 `sys_oper_log` **保留**(`user_id` 不做引用约束——日志的语义是"当时发生了什么",用户删了日志也要留) |
| 角色 | 校验:`is_default = 1` 的角色不允许删除(它是套餐的投影,见 5.2.1);有用户持有时拒绝删除(要求先解绑,避免静默剥夺用户权限)。级联删 `sys_role_menu`、`sys_role_dept`、`sys_user_role`,并 INCR 权限版本号 |
| 部门 | 校验:有子部门或有关联用户时拒绝删除(不级联删除——部门树被静默裁剪是数据事故)。无引用则直接删 |
| 菜单 | 校验:有子菜单时拒绝;`is_platform = 1` 的菜单只允许超管操作。被套餐(`sys_package_menu`)或角色(`sys_role_menu`)引用时**允许删除,但必须在同一事务里级联清理这两张关联表,并对所有租户 INCR 权限版本号**(菜单是全局数据,删掉要让所有租户立刻看到结果) |
| 套餐 | **不做物理删除**,只能 `status = 0` 禁用(有租户在用时删掉会让 `tenant.package_id` 指向不存在的记录)。禁用后不可再被新租户选用,不影响已绑定租户 |
| 租户 | **不做物理删除**,只有禁用(见 4.11)。租户数据量级大且涉及跨表清理,现阶段不提供该能力,列入第 10 节遗留 |
| 字典类型 | 级联删对应 `sys_dict_data`(按 `dict_type`),并清理字典缓存 |

---

## 6. 脱离 HTTP 请求周期的场景

`TenantWebFilter` 依赖 Sa-Token 会话,以下场景没有会话,必须显式确定并设置租户上下文(用 `TenantContext.runAsTenant`),**不允许用超管身份(`isSuperUser = true`)跳过过滤图省事**——那等于给这些入口开了能看到全租户数据的后门。

### 6.1 支付回调(本版只保留设计,不实现)
回调本身没有登录态,租户识别方式:
- **推荐**:把 `tenant_id` 编码进商户订单号。**格式必须定死**:`{tenantIdBase36}-{渠道编码}-{渠道流水号}`,总长不超过 32 字符(如 `1a2b3c-wx-202609181234567890`)。用 Base36 而不是十进制,是为了在同样长度里塞下雪花 ID,且不像自增 ID 那样容易被猜出数据量(与 4.3 的取向一致)。**解析失败(格式不对/长度超限)的回调直接拒绝并记告警日志**,不做"容错猜测"
- **备选**(订单号格式被渠道定死、改不了时):查询 `sys_pay_account`——一张平台级表,存"支付账号 → 租户"的映射(`pay_account_id`、`channel`、`tenant_id`)。它不是租户表,本来就该有租户维度,查它不算绕过隔离。**建表脚本已包含这张表**(前一版只在正文提到、脚本里没有,属于文档与脚本不一致,已修正)
- **两者的共同前提是验签**:租户识别解决"这笔回调属于谁",不解决"这条回调是不是渠道发来的"。渠道签名校验失败 → 直接拒绝(401/403),**且必须发生在解析 `tenant_id` 之前**
- **解析出的 `tenant_id` 只是"定位线索",不是可信事实**。正确顺序:验签通过 → 用解析结果 `runAsTenant` 进入上下文 → 按订单号查本地订单表(受租户过滤)→ **查不到就丢弃并告警**。因为订单表受租户过滤,构造一个不存在的租户前缀只会得到"查不到",读不到任何数据;订单归属一律以本地订单记录里的 `tenant_id` 为准,不用订单号里的值去写数据
- 拿到可信的租户上下文后处理业务,**不要用超管模式处理回调**(那是给"能看到全租户数据"开后门,回调不需要这个能力)

### 6.2 定时任务 ✅
`@Scheduled` 方法运行在独立任务线程,进入方法体时 `TenantContext` 为空:
- 处理"所有租户"的任务(如每日结算):遍历 `tenant` 列表(这个查询本身是平台级的,不涉及租户过滤),逐个 `runAsTenant`,不要一次性开超管跑全部——单租户失败不影响其他租户,方便按租户重试
- 只处理单一租户的任务:直接 `runAsTenant(tenantId, ...)`。这类任务(如 4.8.1 的按租户同步)同样是**每个租户一个事务**,循环里 try/catch 单租户异常,记日志后继续
- **多实例部署注意**:`@Scheduled` 在每个实例上都会触发,同一批租户会被处理多遍。现阶段假设单实例部署;确实要多实例时,需要 Redis 分布式锁或调度中心,本方案不展开,列入第 10 节遗留

**落地**:`TenantTaskRunner`(service/sys/support)把上面这段循环固定成骨架:逐个租户 `runAsTenant`、每租户独立 `try/catch`、跳过禁用/过期租户、返回成功数与失败租户清单。两个必须知道的行为:①**在事务内调用会被直接拒绝**——否则所有租户共用一个事务,单租户失败会把整批带回滚,与本节语义正好相反;②业务侧只写"单个租户要做什么",且那个方法**必须标注 `@Transactional`**(6.3 第 1 条),否则没有 Session 边界、过滤器无处可挂。

### 6.3 线程池场景 ✅
`@Async`、任务线程池提交的任务里,ThreadLocal 不跨线程传播,`runAsTenant` 必须在**目标线程内部**调用,不能在提交任务前的调用方线程里调。

补充三条硬约束(都是前面机制的必然推论,不满足就等于隔离失效):

1. **异步入口方法必须标注 `@Transactional`**。没有事务就没有 Session 边界,4.2 的事务切面没地方 enable 过滤器,隔离静默失效
2. **审计信息靠参数传入,不靠 ThreadLocal**:提交任务时把 `AuditContext` 快照(见 4.12)作为参数传进异步方法,在里面显式给 `tenant_id`/`create_by` 赋值。不要在异步线程里读 Sa-Token 会话——那里没有
3. **MDC(traceId)同样不跨线程**:需要日志里保留 traceId 的线程池,用自定义 `TaskDecorator` 显式拷贝 MDC(见 7.2)

**落地**:`ContextPropagatingTaskDecorator` + `AsyncConfig`(Bean 名 `applicationTaskExecutor`,用虚拟线程执行并挂上装饰器,`@Async` 直接可用)。装饰器**只拷 MDC,不拷租户与审计上下文**——把租户顺手带过去会让"忘记设置租户"静默变成"看起来正常",另一面就是串到上一个请求的租户;执行完恢复现场,避免 traceId 泄漏给下一个任务。操作日志的写入线程同样处理(`OperLogWriter` 用日志条目里的 traceId 快照补回 MDC,结束时还原),否则异步落库自己产生的日志行 traceId 是空的。

---

## 7. 通用规范

### 7.1 安全

- 密码 BCrypt 加密存储
- 登录接口:失败次数达到阈值锁定 + IP 维度限流,具体契约见 7.1.1
- 敏感字段(手机号/身份证)返回值和日志都要脱敏

#### 7.1.1 登录接口契约 ✅

**为什么要单独把这个写清楚**:登录是唯一一个"发生在 `TenantWebFilter` 之前"的接口——这时候还没有登录态,`TenantContext` 里什么都没有,但登录逻辑本身又必须先知道"这是哪个租户的哪个用户",所以租户识别这一步不能依赖后面通用的过滤链路,必须在登录接口内部单独处理。这是本方案里少数几个需要给出具体数值/具体字段名的地方,不再只描述"职责",避免实现方在这几个点上各自发挥导致行为不一致。

**端点**:`POST /auth/login`,公开接口(不经过 Sa-Token 登录态校验,也不经过 4.2 的租户过滤——此时还没有 tenantId 可用)

**请求体**:
```json
{
  "tenantCode": "acme",
  "username": "admin",
  "password": "明文密码,由 HTTPS 保证传输安全,服务端不做额外加密,直接和 sys_user.password 的 BCrypt hash 比对"
}
```
**为什么要显式传 `tenantCode`**:`sys_user` 的用户名唯一性是 `(tenant_id, username)` 联合唯一(建表脚本 `uk_tenant_username`),同一个用户名在不同租户下允许重复,所以登录时只凭 username 查不到唯一的用户,必须先确定租户。用 `tenantCode` 对应 `tenant.tenant_code` 字段是最直接的方式;如果以后要做"按子域名自动识别租户"这类前端部署层面的机制,登录接口这一层不用改,只是 `tenantCode` 从用户手填变成前端根据域名自动带上,不影响后端契约。

**处理顺序**:
1. 按 `tenantCode` 查 `tenant` 表:租户不存在,或 `status = 0`(禁用),或已过 `expire_time`——都返回同一句"用户名或密码错误",不单独提示"租户不存在/已过期",避免被用来批量探测有效的 `tenantCode`。**这一步复用 4.11 的租户状态缓存**:按 `tenantCode` 查租户时走 Redis(`tenant:code:{tenantCode}` → tenantId,TTL 同样 60 秒),否则登录接口会把 `tenant` 表打成热点,而且每次撞库都会查库
2. 按 `(tenant_id, username)` 查 `sys_user`:不存在同样返回上面这句统一文案
3. 检查 `sys_user.lock_time`:非空且晚于当前时间,直接返回"账号已锁定,请稍后重试",**不再往下比对密码**——如果锁定期内还根据密码对错返回不同提示,等于变相泄露了"这个账号是否存在/密码是否猜对"
4. BCrypt 比对密码:
   - 失败:`login_fail_count += 1`;达到 **5 次**时,设置 `lock_time = 当前时间 + 15 分钟`,并把 `login_fail_count` 归零重新计数(不做"锁定时长随失败次数递增"这种升级策略,现阶段固定15分钟,是否要升级列入以后按需处理,不在本次方案展开)
   - 成功:`login_fail_count` 归零、`lock_time` 置空,继续第5步
5. 调用 Sa-Token `StpUtil.login(userId)`,并把 `tenantId`、`isSuperUser`(取自 **`sys_user.is_super`**,不是 `tenant.is_super`,见 4.1/4.10)、`userId` 写入该次登录的 Sa-Token session 扩展数据——`TenantWebFilter`(4.2 节)后续所有请求都从这里读,不是每次都重新查库。**权限集合不写进会话**,走独立缓存(见 5.5)
6. 若 `sys_user.must_change_password = 1`,响应里带 `mustChangePassword: true`,前端强制跳改密页;服务端侧由 5.4 链路的拦截器兜底(除改密/登出/刷新权限外一律 403),不依赖前端配合

**IP 维度限流**:同一 IP **每分钟最多 10 次**登录请求,不区分租户和用户名(防的是自动化撞库脚本,不是针对单个账号),超过直接返回 429,请求根本不进入上面的账号级校验逻辑。这是和账号锁定(第4步的5次阈值)完全独立的两层防护,不要合并成一套计数器——一个防止"扫大量账号",一个防止"死磕一个账号"。

**成功响应**:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "token": "访问令牌(Sa-Token 生成的 token 值)",
    "refreshToken": "刷新令牌,客户端必须持久化,用于访问令牌过期后换新的令牌对(见 7.1.3)",
    "userId": "...",
    "tenantId": "...",
    "isSuperUser": false,
    "mustChangePassword": false,
    "nickname": "...",
    "menus": [ "..." ],
    "permCodes": [ "order:delete", "..." ]
  }
}
```
`menus`/`permCodes` 是按 5.2 节的菜单权限过滤后的结果,前端直接用来渲染动态路由(7.4节)和控制按钮显示,不需要登录后再调一次接口。**注意这是登录那一刻的快照**:权限变更后要靠重新登录或调用 `GET /auth/permissions` 刷新(见 5.5),服务端不认识前端缓存的这份快照,校验始终以服务端为准。

**登出**:`POST /auth/logout` → `StpUtil.logout()` 清理 Sa-Token 会话,**并撤销本次登录对应的刷新令牌**(见 7.1.3);不涉及业务表数据变更。

**Sa-Token 关键配置(给具体取值,不留白)**:
```yaml
sa-token:
  token-name: Authorization
  token-prefix: Bearer    # 客户端按 Authorization: Bearer <token> 传入,读时自动去前缀
  timeout: 1800          # 访问令牌 TTL,单位秒(30分钟)。**短 TTL 是刷新机制生效的前提**,见 7.1.3
  active-timeout: -1     # 不启用滑动过期:续期必须走显式刷新接口,否则无法在续期时做安全判断
  is-concurrent: true    # 允许同一账号多端同时登录
  is-share: false        # 每端拿到的token不同,便于精确踢单端下线,而不是踢掉所有端
  token-style: uuid
  is-read-header: true
  is-read-cookie: false  # 前后端分离部署,不依赖cookie传token
```

#### 7.1.3 刷新令牌(refresh token) ✅

**为什么不直接用 Sa-Token 的滑动过期**:`active-timeout` 确实能"有请求就续期",但它解决不了两个实际问题——
①**续期时无法做安全判断**:只要请求没断,一个被盗的令牌可以无限续下去,永远是有效的;
②客户端拿不到一个明确的"可刷新凭据",做不了"到期前主动静默续期"。
所以这里用显式的双令牌:访问令牌短命、刷新令牌长命且可撤销。

| 令牌 | 载体 | TTL(prod) | 用途 |
|---|---|---|---|
| 访问令牌(access token) | Sa-Token 会话 token,`Authorization` 头 | **1800 秒** | 每个业务请求都带 |
| 刷新令牌(refresh token) | 服务端生成的 opaque 随机串(**不用 JWT**) | **7 天** | 只用于换新的访问令牌 |

**刷新令牌为什么用随机串 + Redis,而不是 JWT**:它必须**能被服务端撤销**——登出、改密、禁用用户、禁用租户
这四种事件都要让它立即失效(见下面的撤销时机表)。JWT 的无状态特性在这里是负资产:真要撤销还得维护一份
吊销名单,等于把 Redis 又加回来,却额外背了一个签名体系和"算法/密钥轮换"的维护面。

**访问令牌为什么必须短**:刷新机制的收益就来自"泄露的访问令牌会在短时间内自动失效"。如果访问令牌仍是 2 小时,
刷新只是多了一层壳、安全性没有任何提升。短 TTL 的代价是刷新变频繁,由客户端**在收到 401 时刷新一次**来消化
(不要做定时轮询式刷新)。

**关键设计:一次性使用(轮换)+ 重放检测**

- 每次刷新都签发**新的刷新令牌**,旧的立即作废。客户端必须持久化最新那个。
- 服务端为已用过的令牌保留一个**短宽限期标记**(默认 120 秒,只为容忍"网络超时后客户端重试")。
  宽限期内再次使用同一个旧令牌 → 判定为**令牌重放** → **撤销该用户全部刷新令牌 + 踢掉该用户全部会话**
  (`StpUtil.logout(userId)`),强制重新登录。这是刻意的"宁可误伤":代价是用户重新登录一次,
  收益是不放过一个可能已经泄露的令牌。
- 宽限期过后标记自然过期,旧令牌再拿来用就是"不存在的令牌",同样 401。

**端点契约**

请求:`POST /auth/refresh`
```json
{ "refreshToken": "客户端保存的刷新令牌" }
```

这个接口**必须放进 `minimall.tenant.public-paths` 白名单**——调用它的时候访问令牌通常已经过期,
Sa-Token 的登录态校验必然失败。它的租户上下文**来自刷新令牌的载荷**,不来自会话,所以要显式
`runAsTenant(...)` 进入租户上下文,和支付回调(6.1)是同一类处理方式,**不要用超管模式**。

处理顺序:
1. 按 `auth:rt:{refreshToken}` 取载荷(userId、tenantId、isSuperUser)
2. 取不到载荷时:若命中宽限期已用标记 → 走上面的重放处置;否则一律按"凭据无效"失败
3. **校验租户状态(4.11)与用户状态**:租户被禁用/已过期、用户被禁用 → 撤销该用户全部刷新令牌后失败
4. `runAsTenant(tenantId, isSuperUser, ...)` 进入上下文
5. `StpUtil.login(userId)` 签发新访问令牌,并把 `tenantId`/`isSuperUser`/`userId` 写入新会话扩展数据(与 7.1.1 第 5 步一致)
6. 轮换刷新令牌:旧令牌标记为已用(带宽限期),写入新的 `auth:rt:{新令牌}`,更新"用户 → 有效令牌集合"索引
7. 返回新令牌对

响应:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "token": "新的访问令牌",
    "refreshToken": "新的刷新令牌",
    "expiresIn": 1800
  }
}
```

**失败语义**:一律 401 + 同一句"登录状态已失效,请重新登录",**不区分"过期/不存在/已被撤销"**——
区分开来等于免费提供了一个探测接口(与 7.1.1 里登录失败统一文案是同一个取向)。
客户端收到这个 401 就跳登录页,**不要自动重试**(重试会连续触发重放判定,把正常用户踢下线)。

**撤销时机(必须全部覆盖,漏掉任何一个,那个入口的失效就等于没做)**

| 事件 | 处理 |
|---|---|
| 登出 | 撤销本次登录的刷新令牌(客户端请求体带上 refreshToken;不带则按 userId 撤销该用户全部,由实现按"端"信息决定粒度) |
| 修改密码成功(7.1.2) | 撤销该用户**全部**刷新令牌 + 踢掉全部会话(密码变了,所有凭据都该失效) |
| 用户被禁用(7.1.2) | 撤销该用户全部刷新令牌 |
| 租户被禁用/过期(4.11) | 刷新接口第 3 步会拦下;另外超管禁用租户时应主动撤销该租户全部用户的刷新令牌 |

**Redis 索引结构**

| Key | Value | TTL |
|---|---|---|
| `auth:rt:{refreshToken}` | 载荷(userId/tenantId/isSuperUser/签发时间),JSON | 刷新令牌 TTL |
| `auth:rt:used:{refreshToken}` | 载荷(**重放时要靠它定位撤销谁**,所以不能只存一个 1) | 宽限期(默认 120s) |
| `auth:rt:user:{userId}` | 该用户当前有效的刷新令牌集合 | 刷新令牌 TTL |

**多端**:`is-concurrent = true` + `is-share = false`,每个端的登录各自持有独立的刷新令牌、互不影响;
只有"撤销该用户全部令牌"时才一起清掉。登录请求可带 `deviceId`(不传则由服务端生成并随登录响应返回),
它**只用于日志与客服排查,不参与鉴权判断**。

**刷新后旧访问令牌的状态**:**继续有效直到自然过期**(最长 1800 秒)。这是有意的取舍——主动吊销旧访问令牌会让
"客户端重试期间新旧令牌并存"的场景出现随机 401,排查成本高于收益;把访问令牌 TTL 压到 30 分钟,
暴露窗口已经被控制住了。

**需要覆盖的测试用例**(补充到 8.1):
- 访问令牌过期后,用有效刷新令牌能换到新令牌对,且新访问令牌可以正常访问受保护接口
- 刷新返回的新刷新令牌与旧的不同;旧令牌超出宽限期后不可再用
- 宽限期内用旧刷新令牌再次刷新 → 断言该用户**全部会话被踢掉、全部刷新令牌被撤销**
- 修改密码成功后,原刷新令牌立即失效
- 用户被禁用后,原刷新令牌立即失效
- 租户被禁用后,刷新接口返回 401(而不是换出一张仍然可用的新令牌)
- 不带 `Authorization` 头也能调用刷新接口(证明它在白名单里,且租户上下文来自令牌载荷而非会话)

#### 7.1.2 密码策略与账号生命周期 ✅

前一版只写了"BCrypt",没说密码从哪来、怎么改、忘了怎么办。补齐:

- **存储**:BCrypt(cost 10)。`sys_user.password` 存 hash,任何接口都不返回该字段,日志不打印
- **复杂度**:长度 8–32,必须同时包含字母和数字。现阶段不上特殊字符/弱密码字典校验(把"弱密码问题"变成"用户把密码写在便签上"是更常见的失败方式);要升级时改这一节,不在代码里散落判断
- **初始密码**:建租户时由超管录入或系统随机生成;**随机生成时必须置 `must_change_password = 1`**,明文只在创建响应里返回一次,不落库、不写日志
- **改密**:`POST /auth/password`,必填旧密码 + 新密码,校验复杂度、校验新密码不等于旧密码;成功后更新 `pwd_update_time = now`、`must_change_password = 0`,并**强制该用户全部会话失效**(`StpUtil.logout(userId)`)——密码变了旧会话必须失效,这条不能省
- **重置密码**(超管):`POST /users/{id}/password/reset`,走和"随机生成"相同的路径(返回一次明文 + 置 `must_change_password = 1`)
- **强制改密拦截**:`must_change_password = 1` 的会话,除 `/auth/password`、`/auth/logout`、`/auth/permissions` 外的所有请求返回 403 + 明确文案,由 5.4 链路的拦截器实现(不靠前端自觉)
- **账号禁用**:`sys_user.status = 0` 后,该用户的在线会话按 4.11 同款方式处理(状态缓存 + 惰性踢下线)。这条容易漏——"禁用用户后会话仍能用"和租户禁用是同一类问题
- **明确不做**:密码历史复用校验、密码过期强制更换、多因素认证,列入第 10 节遗留

### 7.2 可观测性 ✅

- 全局异常处理,区分业务异常/系统异常/参数校验异常,统一响应码规范
- **traceId**:由 `TraceIdFilter` 在链路最前面生成(若请求头已带 `X-Trace-Id`,例如来自网关,则复用),写入 MDC 并回写响应头 `X-Trace-Id`,方便把前端报错和后端日志对起来。**跨线程不自动传播**:线程池/`@Async` 场景用自定义 `TaskDecorator` 显式拷贝 MDC(见 6.3);异步落库的日志条目要带提交任务时捕获的 traceId,否则异步日志失去关联性,排查跨租户问题时恰好最难用
- **操作日志(`sys_oper_log`)异步落库**,不阻塞主流程。三处必须说清楚:
  - `tenant_id`/`user_id` **由审计快照显式赋值**(见 4.12)。这两列在建表脚本里已改为允许为空,覆盖"登录失败"这类还没识别出用户的场景——那时 `user_id` 为空、`tenant_id` 也可能为空,不能因为非空约束把最有排查价值的一条日志丢掉
  - **日志内容必须脱敏**:切面写 `request_params` 前按字段黑名单剔除/掩码(`password`、`oldPassword`、`newPassword`、`token`、`secret`、`idCard` 等),手机号按 7.1 的脱敏规则处理;`request_params` 与 `error_msg` 都截断到 4000 字符上限,避免大报文撑爆日志表。**登录接口的请求体绝不能原样落库**——它包含明文密码
  - 异步落库失败**不能影响主流程**,但必须记 WARN 日志(否则日志表静默停止写入没人知道)

### 7.3 接口规范 ✅

- 统一响应体 `{code, message, data}`,分页格式统一
- 关键写接口(支付、订单创建)必须幂等设计
- 参数校验用 Bean Validation,不手写散落的 if 判断
- **按 ID 操作的越权防护(强制)**:所有"按 ID 读/改/删"的接口,必须先经过滤器生效的查询把实体加载出来(`findById` 在过滤器启用时已自动带租户条件,加载不到即返回"资源不存在"),再对加载到的实体做修改。**禁止**两种写法:①`update ... where id = :id` 这类 JPQL/native bulk 语句直接改(不受过滤器保护,见第 2 节的风险兜底);②先查后改但把实体跨出了过滤器生效的作用域。统一返回"资源不存在",不区分措辞——避免通过错误文案区分"数据存在但不属于你"和"数据不存在"
- **时间类型**:数据库统一 `DATETIME`,Java 侧统一 `LocalDateTime`,业务时区固定 `Asia/Shanghai`(JDBC 连接串与 JVM 都显式指定,不依赖机器默认值)。跨时区多区域部署不在本次范围
- 错误码分段固定:业务异常用业务码段、系统异常统一码、参数校验统一码;具体数值由实现方定,但**同类异常的码值集中定义在一处**,不在 Controller 里散落魔法数字

### 7.4 前端配套
- 菜单表驱动动态路由,不是前端写死
- 按钮权限指令对应后端 `perm_code`
- **权限快照的刷新**:登录时拿到 `menus`/`permCodes` 后缓存,收到 403 时调用 `GET /auth/permissions` 刷新一次(见 5.5);仍失败再引导重新登录,避免"权限已变更但页面按钮还在"的误导
- **公开接口要带租户标识**:调用 4.9 白名单之外的公开接口时,请求头统一带 `X-Tenant-Code`(取自当前站点配置),否则会被兜底规则拒绝

---

## 8. 测试计划

测试和上面的设计是同等优先级的交付物,不是后置项。以下用例描述"验证什么、期望结果是什么",具体测试代码由实现方编写。

### 8.1 隔离性测试(CI 必跑,不可跳过) ✅

**落地索引**(23 条都已有对应用例):

| 用例 | 用例类 |
|---|---|
| 1、2、6、7 租户隔离、超管可见全部、未定租户默认拒绝 | `TenantIsolationTest` |
| 3、4、5、8 数据权限各档位、跨部门越权、归属人阻断 | `DataScopeIntegrationTest` |
| 9、10、11 登录、强制改密闸门、改密踢掉全部会话 | `AuthHttpFlowIntegrationTest` |
| 12~16 建租户六步、越界授权、降级/升级、平台改套餐内容、重同步出口 | `TenantProvisioningIntegrationTest` |
| 17、18 权限收紧即时生效、租户禁用后在线会话失效 | `PermissionMatrixIntegrationTest`、`AuthHttpFlowIntegrationTest` |
| 19 变更记录与实际生效一致 | `TenantProvisioningIntegrationTest` |
| 20~23 刷新轮换与重放、刷新时校验租户/用户状态、改密撤销刷新令牌、刷新接口在白名单 | `RefreshTokenRotationTest`、`AuthHttpFlowIntegrationTest` |

用例是**刻意分散**的:放在"被验证行为所属的层"里(纯 Redis 逻辑不启 Spring 上下文,HTTP 口径必须走真实请求),而不是按编号堆成一个大文件。

1. 造两个租户 A、B 各自的数据,租户 A 登录态下查询,断言看不到租户 B 的任何数据
2. 平台超管账号(`sys_user.is_super = 1`)登录态下查询,断言能看到 A、B 两个租户的全部数据;**再断言该超管租户下的普通用户只能看到自己租户的数据**(这是 4.10 改用户级粒度的核心回归点)
3. 租户 A 尝试通过接口参数伪造访问租户 B 的具体资源 ID,断言返回统一的"资源不存在",不是返回租户 B 的数据
4. **同租户跨部门越权**:租户 A 内 `data_scope = 2`(本部门)的用户尝试按 ID 修改其他部门的数据,断言被拒绝。第 3 条只覆盖跨租户,而这一条才是日常最高频的越权场景
5. 新增数据时不显式传 `tenant_id`,断言自动回填为当前登录租户的 ID
6. **异步场景过滤生效性**:模拟一次 `runAsTenant` 调用(不经过 HTTP 请求),在其中查询数据,断言只返回指定租户的数据——专门防止"上下文设对了但 Hibernate 层没生效"的回归(4.2 的切面顺序写错时,只有这条能测出来)
7. **未定租户默认拒绝**:清空 `TenantContext` 后查询租户表,断言返回空集(而不是全表);置 `isSuperUser = true` 后断言返回全量
8. `sys_role` 查询:租户 A 登录态下查角色列表,断言只能看到租户 A 自己的角色,看不到租户 B 的角色(普通的 `tenant_id = :tenantId` 等值过滤,不再有 OR 平台模板的特殊逻辑)
9. **多角色 `data_scope` 取最大**:给用户同时挂"仅本人"和"本部门"两个角色,断言按"本部门"返回;再叠加一个"全部"角色,断言按"全部"返回
10. **`data_scope = 4` 多角色部门并集**:两个角色各配不同部门,断言返回两个部门的并集数据
11. **归属人为空时写入被阻断**:在无审计上下文的异步线程里插入参与数据权限的业务数据且不显式赋值,断言抛异常(而不是写入 `create_by = null` 的脏数据)
12. **租户创建完整性**(4.7):用套餐 P(菜单集合 M)创建新租户,断言默认管理员角色拥有且仅拥有 M 里的菜单权限(不多不少)、默认管理员用户已创建并绑定了根部门、根部门已创建
13. **越界授权必须被拒绝**(5.2.1):租户管理员提交一个不在其套餐内的 `menu_id`,断言返回业务异常,且 `sys_role_menu` 里没有新增该记录
14. **套餐降级立即收权**(4.8):用 P1(M1)创建租户后变更为 P2(M2 ⊂ M1),断言默认管理员角色和该租户下**所有**自定义角色的 `sys_role_menu` 里都不再包含 `M1 - M2`
15. **套餐升级只影响默认角色**(4.8):M2 ⊃ M1 时,断言新增菜单只出现在默认管理员角色,自定义角色的菜单集合前后一致
16. **平台改套餐内容立即生效**(4.8.1):修改 `sys_package_menu` 增删菜单,断言所有绑定该套餐的租户按 4.8 规则同步完毕,`sys_tenant_package_change` 记录了 `trigger_type = 2`;并断言其中一个租户的同步失败时其他租户不受影响(按租户独立事务)
17. **租户禁用后在线会话立即失效**(4.11):租户 A 的用户先登录拿到 token,再把租户 `status` 置 0 并删除状态缓存,用**同一个 token** 请求,断言返回 401 且会话已清理
18. **权限收紧立即生效**(5.5):用户登录后已有权限缓存,收回其某个角色的菜单授权(触发版本号 INCR),断言该用户**同一个 token** 的下一次请求即被拒(403),不需要重新登录
19. **变更记录一致性**:`sys_tenant_package_change` 的 `added_menu_ids`/`revoked_menu_ids` 计算结果与实际生效的差异一致
20. **刷新令牌轮换与重放检测**(7.1.3):用刷新令牌换到新令牌对后,断言新访问令牌能访问受保护接口;随后在宽限期内用**同一个旧刷新令牌**再次刷新,断言该用户全部会话被踢掉、全部刷新令牌被撤销
21. **刷新时校验租户与用户状态**(7.1.3、4.11):租户被禁用、用户被禁用两种情况下,刷新接口都返回 401,而不是换出一张仍然可用的新令牌
22. **改密撤销刷新令牌**(7.1.2、7.1.3):修改密码成功后,原刷新令牌立即不可用
23. **刷新接口在白名单**(4.9):不带 `Authorization` 头调用 `/auth/refresh`,断言能正常处理——证明它的租户上下文来自令牌载荷,而不是来自会话

### 8.2 权限矩阵测试

列出"角色 × 接口"的预期结果表(通过/403),自动化跑,不靠人工逐条点。至少覆盖四种情况:有权限角色、无权限角色、平台超管(`is_super = 1`,断言短路返回全量权限、不受角色配置影响)、租户管理员越界授权(见 8.1 第 13 条)。

**入口与断言口径**:服务层用 `PermissionProvider.load` 验证"算出来的权限集合对不对"(并集、停用角色、超管短路、无租户默认拒绝),HTTP 层用真实请求验证"通过/403"。**后者不能省** —— 校验发生在拦截器里,只有走完整 MVC 链路才测得到;这也正是它发现"注解没生效"的原因(见 5.2)。

### 8.3 数据权限边界测试

- `data_scope=3`(本部门及以下)在部门层级较深(如4层以上)时,断言递归范围正确,不遗漏不越界
- `data_scope=4`(自定义部门)断言只返回 `sys_role_dept` 里配置的部门数据
- `data_scope=1`(仅本人)断言只返回 `create_by` 为当前用户的数据
- `dept_id` 为空的用户按 2/3 档查询时只能看到自己(而不是全量);**第 4 档不适用这条** —— 它的可见范围来自 `sys_role_dept` 的配置,与用户自己有没有部门无关,所以 4 档要单独断言(按配置部门返回)
- 多角色叠加时的取最大规则(见 8.1 第 9、10 条)

### 8.4 异步场景测试

- 支付回调:构造带租户前缀的 `out_trade_no`,断言解析出的 `tenant_id` 正确,且处理过程中只操作该租户的订单数据;追加三条——**签名错误的回调被拒绝(且在解析租户之前就被拒)**、**订单号里的租户前缀与本地订单记录的 `tenant_id` 不一致时以本地记录为准或直接丢弃告警**、格式非法的订单号被拒绝
- 定时任务:模拟其中一个租户处理抛异常,断言其他租户的处理不受影响(不因为一个租户失败导致整批任务中断);断言每个租户的处理**各自独立提交事务**
- **异步落库**:断言异步线程写入的 `sys_oper_log` 的 `tenant_id`/`user_id` 与提交任务时一致(即审计快照确实传过去了,而不是靠线程内推断)
- **套餐重新同步入口**(4.8.1 的运维出口):对已处于"未同步"状态的租户重跑一次同步,断言能收敛到正确权限状态

**进度**:定时任务的失败隔离与独立事务、异步落库的身份快照、套餐重同步出口三项已有用例(`AsyncScenarioIntegrationTest`、`TenantProvisioningIntegrationTest`);支付回调那项随 6.1 一起暂缓(本版不做业务模块)。

---

## 9. Maven 依赖与多环境配置

### 9.1 依赖清单(职责说明,非 pom.xml 原文) ✅

| 分类 | 坐标(group:artifact) | 说明 |
|---|---|---|
| Web | `spring-boot-starter-web` | |
| 持久层 | `spring-boot-starter-data-jpa` | 带 Hibernate **7.4.x**(Spring Boot 4.1.0 实际锁定 7.4.1;7.1/7.2 那条线对应的是 4.0,前一版写 7.2.x 是错的) |
| QueryDSL | `io.github.openfeign.querydsl:querydsl-jpa`(**无 classifier**)、`querydsl-apt`(classifier `jakarta`,scope provided) | **不要用 `com.querydsl` 官方坐标**——原项目最后一次发版在2021年,基本处于停止维护状态,对 Hibernate 6+ 的兼容性有已知未修复问题;OpenFeign fork 是目前活跃维护、专门适配 Jakarta 命名空间的分支,选型时明确指定用这个。**分类器在这两个 artifact 上不对称,已实测**:fork 7.x 的 `querydsl-jpa` 是 jakarta-only,写 `classifier=jakarta` 会直接解析失败;只有 `querydsl-apt` 需要 `jakarta` 分类器(它同时还有 `jpa`/默认两种,默认那个是 javax 命名空间) |
| 校验 | `spring-boot-starter-validation` | |
| 鉴权 | `cn.dev33:sa-token-spring-boot4-starter`、`cn.dev33:sa-token-redis-template` + `org.apache.commons:commons-pool2`(会话存 Redis) | **两处注意**:①**不是 `sa-token-spring-boot3-starter`**——Sa-Token 给 Spring Boot 4 单独发了适配模块(`sa-token-spring-boot4-starter`),本方案技术栈是 Spring Boot 4.1.x,应该用这个;②**会话存储用 `sa-token-redis-template`,不是前一版写的 `sa-token-redis-jackson`**——Spring Boot 4 起默认 JSON 库换成 Jackson 3(包名改为 `tools.jackson`),Sa-Token 官方"集成 Redis"文档现在推荐的是基于 `RedisTemplate` 的 `sa-token-redis-template`,别再按旧版本记忆去引 jackson 模块。相关插件(redisson 等)二选一,不要两套 `SaTokenDao` 同时引入 |
| 版本硬约束 | — | `sa-token-redis-template` 自 1.46.0 起使用了 Redis 6.0+ 的 `SET KEEPTTL`,**Redis 版本必须 ≥ 6.0**,低于 6.0 会直接报 `ERR syntax error`。集成模块的版本必须与 `sa-token-spring-boot4-starter` 完全一致 |
| 数据库驱动 | `com.mysql:mysql-connector-j` | |
| 缓存 | `spring-boot-starter-data-redis` | |
| 表结构迁移 | **`spring-boot-starter-flyway`**、`flyway-mysql` | **两个都不能少,且 starter 不能用 `flyway-core` 替代**:Spring Boot 4 把自动配置按模块拆了,只有 `flyway-core` 在类路径上**不会触发 Flyway**,现象是启动时一行 Flyway 日志都没有、紧接着 Hibernate `validate` 报 `missing table ...`(极易误判成实体映射问题);`flyway-mysql` 则是 Flyway 10+ 起拆分出来的 MySQL 支持,不引会报"不支持该数据库"。见 9.5 |
| 测试 | `spring-boot-starter-test` | 不用 Testcontainers(本机无容器环境),见 9.4 |
| 架构约束 | `com.tngtech.archunit:archunit-junit5` | 用一条测试固化第 3 节的包依赖方向(违反即构建失败),成本极低,别省 |
| 编译期辅助 | `lombok`(可选,团队若接受) | |

**构建插件要求**:QueryDSL 的 Q 类是编译期通过注解处理器(APT)生成的,`maven-compiler-plugin` 需要在 `annotationProcessorPaths` 里显式声明 `querydsl-apt`(带 jakarta classifier)和 `jakarta.persistence-api`,否则 Q 类不生成,QueryDSL 相关代码编译不过。这是实现时最容易被漏配的一步。

**具体版本号(2026年9月核实,实现时请再核对一遍是否有更新的补丁版本)**:

| 坐标 | 版本 | 备注 |
|---|---|---|
| `org.springframework.boot:spring-boot-starter-parent` | `4.1.0` | 2026年6月10日 GA,当前 Spring Boot 4 系列最新次要版本;之后如果出了 4.1.x 补丁版本,直接用补丁版本最新的,大版本不用跳到 4.2(那是下一个次要版本,可能有不兼容变更)。**该版本锁定 Hibernate 7.4.1 与 Jackson 3**,后者影响 Sa-Token 会话存储模块的选型(见上表) |
| `cn.dev33:sa-token-spring-boot4-starter` 及同系列(`sa-token-redis-template` 等) | `1.46.0` | 2026年8月18日发布(前一版写的 1.45.0 是 2026年3月9日的版本,已过时一个次要版本)。同一个 `sa-token-parent` 下所有子模块统一走这个版本号,不要混用;集成模块(redis-template/redisson)的版本必须和 starter 完全一致 |
| `com.mysql:mysql-connector-j` | `26.7.0` | **版本号看着跳得离谱(9.7.0 直接到 26.x),但这是真的**,是 MySQL 官方在 2026年7月29日 正式发布的版本号重新编号,不是野鸡分支或投毒——去 `dev.mysql.com/doc/relnotes/connector-j/en/` 官方发布说明能直接查到 26.x 系列。之前一版本的方案说明里我曾把这个当成"依赖投毒"的可疑迹象,是没有直接查官方发布页、只看了几条第三方 GitHub 仓库的自动升级记录就下的错误判断,这里更正 |
| `org.flywaydb:flyway-core` / `flyway-mysql` | `12.x`(如 `12.5.0`) | 两个模块版本号必须一致 |
| `io.github.openfeign.querydsl:querydsl-jpa` / `querydsl-apt` | `7.x`(如 `7.5`/`7.6`) | 该 fork 已在适配 Hibernate 7.4.x,与本方案的 Hibernate **7.4.x** 一致(前一版把本方案的 Hibernate 写成 7.2.x,与 4.1.0 实际锁定的版本不符,已更正) |
| `org.projectlombok:lombok` | `1.18.46` | |

以上版本号是核实时点的真实最新版本,不是训练数据里的旧印象——但"最新"本身是会变的,建议实现方在实际写 `pom.xml` 前,直接去 Maven Central / 各项目官方发布页再确认一次当前最新的兼容补丁版本,而不是直接照抄这张表的数字。

### 9.2 多环境配置结构 ✅

采用 `application.yml`(公共配置)+ `application-{profile}.yml`(环境差异)的标准 Spring Profile 结构,三个 profile:`dev`、`test`、`prod`,通过 `spring.profiles.active` 激活,不用 Maven profile 做环境区分(那是给"打包产物本身不同"的场景用的,这里各环境的产物应该是同一个 jar,只是启动时激活的配置不同)。

### 9.3 各环境差异点 ✅

| 配置项 | dev | test | prod | 说明 |
|---|---|---|---|---|
| 数据库 | 同一台 MySQL 8 实例,库名 `xxx_dev` | 同一台 MySQL 8 实例,**独立库名**(如 `xxx_test`),和 dev/prod 物理隔离,不共用数据 | 同一台或独立 MySQL 8 实例,库名 `xxx`(或按部署方式定) | CI 上库名需按流水线隔离,见 9.4 |
| Hibernate `ddl-auto` | `validate`,三环境统一 | `validate` | `validate` |
| SQL 日志 | 开 | 开 | 关,或只在排查问题时临时开 |
| `spring.jpa.open-in-view` | `true`(显式配置并加注释说明理由) | `true` | `true` | 见 4.2:HTTP 路径依赖它保证单 Session,**不能依赖框架默认值**(默认值启用了带 WARN,且属反模式,将来可能被改掉) |
| 业务时区 / 时间类型 | `Asia/Shanghai` + `LocalDateTime` | 同左 | 同左 | JDBC 与 JVM 都显式指定,不依赖机器默认(见 7.3) |
| 租户状态缓存 TTL(4.11) | 60s | 60s | 60s | 兜底收敛时间;禁用租户时主动删缓存 |
| 权限缓存 TTL(5.5) | 与 Sa-Token 会话超时对齐 | 同左 | 同左 | 版本号 key(`perm:version:*`)不设 TTL |
| Redis 版本 | ≥ 6.0(本地开发也一样) | ≥ 6.0 | ≥ 6.0 | `sa-token-redis-template` 1.46.0 使用 `SET KEEPTTL`,低于 6.0 直接报错(见 9.1) |
| Redis | 本地单机 | 本地单机(用独立的 DB index 区分,如 Redis 15 号库专供测试,避免和 dev 数据混在一起) | 生产 Redis 实例/集群 |
| 访问令牌 TTL(`sa-token.timeout`) | 长(如 86400,方便调试不用频繁重新登录) | 1800 | 1800(30 分钟;短 TTL 是刷新机制生效的前提,见 7.1.3) |
| 刷新令牌 TTL | 长(如 30 天,免得反复登录) | 7 天 | 7 天 |
| 刷新令牌重放宽限期 | 120s | 120s | 120s(只为容忍客户端网络重试,不要调大) |
| `SNOWFLAKE_WORKER_ID` | 固定值(单机开发不冲突) | 固定值 | **每个实例必须不同**,多实例部署时用容器编排工具(如 K8s StatefulSet 的序号,或部署脚本注入)分配,不能让两个 prod 实例用同一个值,否则 ID 会撞 |
| actuator 端点暴露 | 全开,方便调试 | 按需 | 只暴露健康检查,其余端点关闭或加鉴权 |
| 敏感配置存放 | 明文写在 `application-dev.yml` | 明文写在 `application-test.yml` | 明文写在 `application-prod.yml`(前面已确认现阶段不接配置中心,这条对 prod 同样适用,不是遗漏) |

**`ddl-auto` 为什么三环境都是 `validate`,不是"dev 宽松、prod 严格"**:一旦引入 Flyway 管理表结构(9.5),Hibernate 就不应该在任何环境自动建表/改表,统一由迁移脚本驱动——如果 dev 环境靠 Hibernate `update` 自动改表,开发者本地的表结构会和迁移脚本描述的状态脱节,迁移脚本本身反而没人验证过,等发布到 prod 才发现脚本有问题。dev 环境启动前先跑 Flyway 迁移,和 test/prod 走同一套流程,这是本方案里唯一一个所有环境都不差异化处理的配置项。

### 9.4 测试环境额外说明

用同实例不同库名代替 Testcontainers,少了容器天然的"用完即焚"隔离,要在测试框架层面补回来:

- 测试库 schema 由 Flyway 按 9.5 的迁移脚本建立,和 dev/prod 走同一套脚本,保证测试环境的表结构和生产是一致的,不会出现"迁移脚本在测试环境没跑过,发布到 prod 才炸"的情况
- 数据清理:每个测试方法用 `@Transactional` 包住,测试结束自动回滚,不真正提交数据;涉及测试内部需要看到已提交数据的场景(比如验证异步任务处理效果),改用显式 truncate 相关表,在 `@AfterEach` 里做,不依赖事务回滚
- 测试库是持久存在的实例,不是每次运行都重新创建,和容器方案比多了一个风险:**测试之间如果清理不干净,会互相污染,且不像容器那样重启就恢复干净状态**,建立测试库清理规范(第8节的隔离性测试用例本身就在测"数据会不会串",如果测试数据先脏了,结果不可信,清理规范要认真执行,不是可选项)
- CI 环境注意:这个方案要求 CI 执行环境能连到同一台 MySQL 实例(网络可达),不是容器方案那种"CI 环境自包含、不依赖外部服务"的模式,如果以后要上 CI 流水线,需要确保 CI runner 和这台 MySQL 之间的网络策略打通,这是本方案没展开的一个环境依赖
- **CI 并发隔离(补充,前一版漏了)**:多个流水线并行时会共用同一个测试库,`flyway migrate` 会互相抢锁,集成测试的数据也会互相污染。落地方式:测试库名由环境变量注入(如 `APP_TEST_DB=xxx_test_ci_${PIPELINE_ID}`),CI 上每个流水线用自己的库名,跑完 `DROP DATABASE`;本地开发固定用 `xxx_test`。9.6 的"迁移脚本健康检查" job 遵循同一套库名规则,避免它和集成测试 job 抢同一个库
- **测试数据源连接池要压小**:测试会启动多个 Spring 上下文(RANDOM_PORT 的 HTTP 端到端用例、事务隔离用例各一个),Hikari 每池默认 10 条连接,几个上下文就能把 MySQL 的 `max_connections` 吃满,现象是 Flyway 报 `Too many connections`、上下文加载失败——很容易被误判成"迁移脚本坏了"。落地方式:surefire 里统一设 `spring.datasource.hikari.maximum-pool-size=3`(见 pom 注释),并确认实例的 `max_connections` 够跑完整个套件
- **sa-token 的全局管理器 + 多上下文**:`SaManager` 是 JVM 全局静态的,而测试 JVM 里同时存在多个 Spring 上下文,每个上下文启动都会把全局 DAO 指向自己的 Redis 连接。那个上下文一旦关闭,后面任何走全局 DAO 的调用(例如 `userService.changeStatus` 里踢会话)都会抛 `LettuceConnectionFactory has been STOPPED`。**生产只有一个上下文,不存在这个问题**;测试侧的处理方式是用当前上下文的 `RedisConnectionFactory` 重新 `init` 一次 DAO(见 `PermissionMatrixIntegrationTest` 的 `@BeforeEach`)

### 9.5 Flyway 迁移脚本组织约定 ✅

- 脚本放在 `src/main/resources/db/migration/`,按 Flyway 默认约定命名:`V{版本号}__{描述}.sql`,如 `V1__init_schema.sql`、`V2__add_audit_columns.sql`,版本号递增,已发布的脚本不能修改,后续变更一律追加新版本文件
- `rbac_tenant_schema.sql`(本方案附带的建表脚本)对应 `V1__init_schema.sql`,是这套脚本的起点
- 应用启动时 Flyway 自动检测并执行未应用的迁移脚本,三环境(dev/test/prod)都走这个流程,不手动跑 SQL
- **种子数据单独一个脚本:`V2__seed_platform_data.sql`**(补充,前一版只说 V1 对应建表脚本,没说"系统起不来"需要的初始数据放哪)。内容:平台租户(`tenant_code = 'platform'`)、平台超管账号(`sys_user.is_super = 1`,密码为固定 BCrypt hash,明文写在脚本注释里并在首次登录后强制修改)、`sys_menu` 完整菜单树(平台专用菜单标 `is_platform = 1`)、一个"全量"套餐(包含全部 `is_platform = 0` 的菜单)、基础字典。理由:`sys_menu` 是平台统一数据,没有它超管登录后连菜单都没有,`V1` 只负责结构
- **种子数据必须幂等**:使用固定的**小整数 ID**(1、2、3…)而不是雪花 ID,配合 `INSERT ... ON DUPLICATE KEY UPDATE` 或 `WHERE NOT EXISTS`,保证重复执行不产生重复数据。这是**全方案唯一使用非雪花 ID 的地方**,理由是种子数据要能被后续脚本稳定引用(如后续版本的 `sys_package_menu` 要引用某个菜单 ID),雪花 ID 每次生成都不同、引用不了;业务数据一律雪花 ID,不能混用
- 后续版本新增菜单/字典,一律追加新的 `Vn__...sql`,**不要改 `V2`**(9.5 开头与 9.6 的"已发布脚本不可变"约定同样适用)

### 9.6 迁移脚本评审与回滚流程 ✅

**评审流程**:迁移脚本(`db/migration/` 下的新文件)作为普通代码变更走 PR,要求至少一名非作者本人 approve 才能合并;CI 里增加一个"迁移脚本健康检查" job,在 9.4 提到的测试库(或一个临时清库)上跑一遍 `flyway migrate`,确认脚本本身语法和版本顺序没问题,复用现有 CI 对 MySQL 实例的网络可达性,不需要额外基础设施。

**Review checklist**(写进 PR 模板,靠人工核对,不是自动化能完全覆盖的):
1. 脚本是否包含破坏性 DDL(`DROP COLUMN`/`DROP TABLE`/字段改名或不兼容的类型变更)——有则必须走下面的"多步兼容"模式,不能在一个脚本里直接做
2. 结构变更(DDL)和数据迁移/补数据(DML)是否混在同一个脚本里——建议拆成两个版本号相邻的脚本,方便定位问题、方便 DML 部分单独重试
3. 大表(结合当前数据量评估)上的 DDL 是否有锁表/耗时风险,MySQL 8 的 Online DDL(`ALGORITHM=INPLACE`)覆盖不了的场景,要不要走停机窗口或第三方在线变更工具——这类工具的选型和操作手册本方案不展开,列入第10节遗留
4. 已发布的脚本(版本号已存在于任何环境)是否被修改——按 9.5 的约定,已发布脚本内容不可变,只能追加新版本文件

**破坏性变更的多步骤模式**:不允许同一个脚本里对已存在的列做 `DROP`/`RENAME`/不兼容类型变更,拆成至少两个版本:
- `Vn`:新增新列/新表,应用代码切到双写或读写新字段,旧列先保留不动
- `Vn+1`(至少等一次完整发布上线、确认没有代码路径再依赖旧列后):专门用来删除旧列的脚本

**回滚策略:"前滚不回滚",不依赖 Flyway 的 undo migration**——Flyway 的自动回滚(undo migration)是付费 Teams 功能,本方案不引入,处理生产迁移失败按以下方式:
1. 迁移必须先在 CI 和预发环境(建议直接用 9.4 的测试库,发布前用生产等量级数据预演一次)跑通,才允许发布到生产
2. 生产迁移失败:Flyway 会把该版本记录为 `failed` 并中断后续迁移。处理顺序是先人工排查失败原因(脚本笔误、目标环境数据和预期不符等),用 `flyway repair` 清除失败记录标记,然后要么发布新版本脚本(`Vn+1`)修正问题,要么确认失败脚本没有产生任何副作用后重新执行——**不修改已发布的 `Vn` 脚本本身**,和 9.5 的"已发布脚本不可变"约定保持一致
3. **最大的风险点**:MySQL 的 DDL 大多不是事务性的,失败可能留下"部分执行"的中间状态,这是"前滚不回滚"策略下没有自动兜底的地方。缓解手段是脚本尽量保持单条语句/小步操作,避免一个脚本里塞多条强相关的 DDL,降低出现"半成功"状态的复杂度和排查难度,不追求自动化解决,依赖前面的 review 和预发布验证降低发生概率

**发布权限**:dev 环境迁移由开发者本地触发;test 环境在 CI 里自动跑;**prod 环境要求先过 review + 预发布验证,再由人工触发发布,不做无人值守的自动迁移**——这是目前团队规模下相对稳妥的做法,不是技术上做不到自动化,而是生产 DDL 失败的处理成本(见上一条)现阶段还不足以支撑完全自动化。

**落地(本版补的两件事)**:

1. **运维命令可用**:pom 里配了 `flyway-maven-plugin`,并把 `flyway-mysql` 与 MySQL 驱动声明为**插件依赖**(插件类路径与项目依赖是分开的,少一个就会报"不支持的数据库")。命令形如
   `mvn flyway:info -Dflyway.url=jdbc:mysql://host:3306/db -Dflyway.user=root -Dflyway.password=...`
   (`validate` / `repair` / `migrate` 同理)。**Windows PowerShell 下每个 `-D` 参数都要单独加引号** —— url 里的 `?`/`&` 会被 shell 当成语法吃掉,表现为"参数没生效、连库用的是当前系统用户、报 Access denied"。不配这个插件,上面那条"用 `flyway repair` 清失败标记"就没有落地手段,只能手改 `flyway_schema_history`,而那是最不该手改的表
2. **健康检查自动化**:`MigrationScriptConventionTest`(命名、版本号唯一且递增 —— 这条尤其重要,不符合约定的文件名会被 Flyway **静默忽略**)+ `FlywayHealthIntegrationTest`(真实库上断言"无待应用版本、无失败记录")。9.4 说的"CI 里加一个迁移脚本健康检查 job",在这个项目里就是这两条测试(CI 上先 `migrate` 再跑测试),不需要额外基础设施

---

### 9.7 数据库设计约定 ✅

- **不使用外键约束**:引用完整性由 service 层保证(见 5.6)。理由:多租户 + 级联删除场景下外键会引入隐式的删除顺序约束和额外锁竞争,而"有引用时该拒绝还是该级联"这类业务判断外键表达不了,最终仍要写在代码里,不如只写一处。**代价是孤儿记录需要靠 5.6 的规则和测试防住**
- **唯一约束显式声明在建表脚本里**(清单见 5.1/下表),不在应用层用"先查后插"保证:应用层校验有并发窗口(两个请求同时通过校验再同时插入),唯一约束是最后一道且可靠。**`sys_menu.perm_code` 必须唯一**,否则 `@SaCheckPermission` 的语义直接失效
- **字符集/引擎**:统一 `InnoDB` + `utf8mb4` + `utf8mb4_0900_ai_ci`,在脚本头部统一声明,不逐表配置
- **时间类型**:统一 `DATETIME`(不用 `TIMESTAMP`,避免 2038 与隐式时区转换),时区约定见 7.3
- **标志位/枚举列统一用 `INT`,不用 `TINYINT`**(实现时踩到的坑,已实测):Hibernate 的 `ddl-auto = validate` 会拿映射的 JDBC 类型与库里的真实类型比对,`Integer` 映射到 `integer`,而 `TINYINT` 是另一种类型,启动即报 `found tinyint but expecting integer`。要保留 `TINYINT` 就只能把实体字段全写成 `Byte`,那会让每一处 DTO/实体边界都出现 `Byte<->Integer` 转换;标志位那点存储收益不值得这份长期噪音,所以统一 `INT`
- **ID 约定**:业务数据一律应用层雪花 ID(见 4.3);只有 9.5 的种子数据用固定小整数
- **索引**:每张租户表的 `tenant_id` 都要有索引(它是过滤条件的第一个字段);关联表要按"被删方向"建索引(如 `sys_role_menu(menu_id)`,因为套餐收回是按 `menu_id` 批量 delete)、`sys_user_role(role_id)`、`sys_dept(parent_id)`、`sys_menu(parent_id)`

---

## 10. 已知遗留

1. **大表 DDL 的在线变更工具选型和停机窗口 runbook 未设计**(见 9.6):目前假设 MySQL 8 Online DDL 能覆盖大部分场景,真遇到覆盖不了的大表变更时,工具选型(如 `pt-online-schema-change`)和操作手册留待按需处理
2. **生产迁移发布目前是"人工触发"**(见 9.6):现阶段团队规模下够用,如果以后团队/发布频率扩大,可以考虑更正式的发布审批流程,现阶段不做
3. **定时任务在多实例部署下会重复执行**(见 6.2):`@Scheduled` 没有分布式互斥,现阶段假设单实例部署;多实例时需要 Redis 分布式锁或调度中心,未设计
4. **租户不支持物理删除**(见 5.6):只提供禁用。若将来有"租户注销需彻底删除数据"的合规要求,需要单独设计
5. **密码安全的高级能力未做**(见 7.1.2):密码历史复用校验、密码过期强制更换、多因素认证
6. **数据权限只覆盖"行级部门范围"**:没有列级权限(同一行不同字段的可见性),脱敏规则目前是硬编码的字段黑名单,不是可配置策略(见 7.2)
7. **超管操作没有双人复核**:能改套餐、能禁用租户的账号是单点,审计只留痕不拦截(见 4.10)
8. **权限缓存的规模上限未压测**(见 5.5):"每用户一个缓存 key + 租户级版本号"的策略避免了批量删除,但 key 数量上限没有实测
9. **健康检查/actuator 的租户维度**:目前健康检查走白名单、不涉及租户数据,未考虑按租户维度的可用性观测(如"某租户的异步任务堆积")
10. **`is_super` 变更后需要手动清一次权限缓存**(见 4.10、5.5):权限缓存按"租户版本号"失效,而 `is_super` 只由种子数据/运维脚本设置,**不经过任何会递增版本号的业务接口**。运维脚本改完这个标记后,该用户可能还带着改之前的缓存值,直到 TTL 到期或该租户版本号因其他操作变化。当前靠"运维改了标记就顺手清缓存"兜着(用例里用 `TransactionTemplate` + 首次读缓存前完成标记变更来规避),后续可以让运维脚本显式调一次失效
11. **测试里遇到的两个 Spring Boot 4 变化**(见 9.4,踩过):默认 JSON 库是 Jackson 3(包名 `tools.jackson`),测试代码里写 `com.fasterxml.jackson.databind` 会编译不过;`TestRestTemplate` 的包路径也变了。HTTP 用例直接用 JDK 的 `java.net.http.HttpClient`,既不受这些变化影响,也少一层依赖
